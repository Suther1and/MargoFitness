# 📚 Git History Guide - Где и как откатываться

## 🎯 Быстрая навигация

### Просмотр истории коммитов

```bash
# Последние 20 коммитов с графом
git log --oneline --graph --all -20

# Детальная информация с изменениями
git log --stat -10

# Только последние 5 коммитов
git log --oneline -5
```

---

## 📜 История коммитов (от новых к старым)

### `f7ee7ca` - FIX: Публичный доступ к тарифам (ТЕКУЩИЙ)
**Дата:** 2024-12-24  
**Что:** Исправлена RLS политика для неавторизованных пользователей

**Изменения:**
- `database/migrations/009_FIX_PRODUCTS_PUBLIC_ACCESS.sql` (20 строк)

**Откат если:** Неавторизованные пользователи НЕ должны видеть тарифы
```bash
git revert f7ee7ca
# ИЛИ
git reset --hard 4ee33c9
```

---

### `4ee33c9` - FEAT: Email, апгрейд, аналитика ⭐
**Дата:** 2024-12-24  
**Что:** Полная реализация email уведомлений, системы апгрейда и аналитики

**Изменения (3198 строк добавлено):**
- ✅ Email сервис: `lib/services/email.ts` (457 строк)
- ✅ Аналитика: `lib/actions/analytics.ts` (402 строки)
- ✅ UI апгрейда: `app/dashboard/upgrade-dialog.tsx` (249 строк)
- ✅ Страница аналитики: `app/admin/analytics/page.tsx` (368 строк)
- ✅ Тестовый email endpoint: `app/api/test-email/route.ts`
- ✅ Документация: 4 файла в `docs/`
- ✅ Package: resend

**Откат если:** Email, апгрейд или аналитика работают некорректно
```bash
git reset --hard 789bd4e
```

⚠️ **ВНИМАНИЕ:** Это большой коммит! После отката потеряешь:
- Email уведомления
- Систему апгрейда
- Аналитику в админке

---

### `789bd4e` - REFACTOR: Оптимизация проекта
**Дата:** 2024-12-23  
**Что:** Удалено 34 файла (-28%), очистка от дублирующей документации

**Изменения:**
- Удалено: 5276 строк (старые MD, SQL файлы)
- Добавлено: 604 строки (OPTIMIZATION_REPORT.md)
- Бизнес-логика сохранена

**Откат если:** Нужны старые файлы документации
```bash
git reset --hard 69d9f63
```

---

### `69d9f63` - DOCS: Payment System README
**Дата:** 2024-12-23  
**Что:** Документация платежной системы (342 строки)

**Изменения:**
- `PAYMENT_SYSTEM_README.md`

**Откат если:** Не нужна документация
```bash
git reset --hard e1dd1d1
```

---

### `e1dd1d1` - FEAT: Cron job + документация
**Дата:** 2024-12-23  
**Что:** Автопродление подписок + полная документация

**Изменения (1082 строки):**
- ✅ Cron job: `app/api/cron/renew-subscriptions/route.ts`
- ✅ `CRON_SETUP.md`, `ENV_SETUP.md`, `TESTING_GUIDE.md`, `WEBHOOK_SETUP.md`
- ✅ `vercel.json` (настройка cron)

**Откат если:** Проблемы с автопродлением
```bash
git reset --hard 5c464c5
```

---

### `5c464c5` - FEAT: Админ-панель управления подписками
**Дата:** 2024-12-23  
**Что:** Страница управления пользователями для админа

**Откат если:** Админ-панель работает некорректно
```bash
git reset --hard 369e326
```

---

### `369e326` - FEAT: Страница оплаты + Dashboard
**Дата:** 2024-12-23  
**Что:** Mock виджет оплаты, обновленный dashboard

**Откат если:** Проблемы с оплатой
```bash
git reset --hard 133d237
```

---

### `133d237` - FEAT: ЮKassa интеграция ⭐
**Дата:** 2024-12-23  
**Что:** Полная интеграция ЮКасса (API, сервисы, миграция БД)

**Откат если:** Платежи полностью сломались
```bash
git reset --hard 34afc06
```

⚠️ **ВНИМАНИЕ:** После отката потеряешь ВСЮ платежную систему!

---

## 🔄 Команды для отката

### 1. Мягкий откат (создает новый коммит)
```bash
# Откатывает изменения конкретного коммита
git revert <commit-hash>

# Пример: откатить последний коммит
git revert HEAD
```

**Плюсы:** История сохраняется, безопасно  
**Минусы:** Создает дополнительный коммит

---

### 2. Жесткий откат (удаляет историю)
```bash
# Откатывается к конкретному коммиту, удаляя все после него
git reset --hard <commit-hash>

# Пример: откатиться к коммиту 789bd4e
git reset --hard 789bd4e
```

**Плюсы:** Чисто, как будто последующих коммитов не было  
**Минусы:** ⚠️ НЕОБРАТИМО! История теряется

---

### 3. Мягкий откат с сохранением изменений
```bash
# Откатывает коммиты, но оставляет изменения в файлах
git reset --soft <commit-hash>

# Пример
git reset --soft 4ee33c9
```

**Плюсы:** Можно пересобрать коммиты  
**Минусы:** Нужно разбираться что делать с изменениями

---

## 📋 Как понять что в коммите?

### Просмотр изменений в конкретном коммите:
```bash
# Показать все изменения
git show <commit-hash>

# Только статистику (какие файлы изменены)
git show --stat <commit-hash>

# Список файлов
git diff-tree --no-commit-id --name-only -r <commit-hash>
```

### Сравнение двух коммитов:
```bash
# Что изменилось между коммитами
git diff 789bd4e..4ee33c9

# Только список файлов
git diff --name-only 789bd4e..4ee33c9
```

---

## 🎯 Рекомендации по откату

### Если проблема в последнем коммите:
```bash
# Откатить последний коммит (безопасно)
git revert HEAD
```

### Если нужно вернуться на несколько коммитов назад:
```bash
# 1. Сначала посмотри что потеряешь
git log --oneline HEAD~3..HEAD

# 2. Создай резервную ветку (на всякий случай)
git branch backup-before-reset

# 3. Откатись
git reset --hard HEAD~3
```

### Если передумал после жесткого отката:
```bash
# Найти потерянный коммит
git reflog

# Вернуться к нему
git reset --hard <commit-hash-from-reflog>
```

---

## ⚠️ ВАЖНЫЕ ПРАВИЛА

1. **Перед любым откатом создавай backup ветку:**
   ```bash
   git branch backup-$(date +%Y%m%d)
   ```

2. **Используй `git revert` вместо `git reset --hard` когда возможно**
   - Это безопаснее
   - История сохраняется

3. **Перед откатом коммить все изменения:**
   ```bash
   git status
   git add -A
   git commit -m "WIP: before rollback"
   ```

4. **Если работаешь с удаленным репозиторием:**
   - НЕ используй `git reset --hard` после push
   - Используй только `git revert`

---

## 📌 Быстрая шпаргалка

| Задача | Команда |
|--------|---------|
| Посмотреть историю | `git log --oneline -20` |
| Откатить последний коммит (безопасно) | `git revert HEAD` |
| Откатиться к конкретному коммиту | `git reset --hard <hash>` |
| Посмотреть что в коммите | `git show <hash>` |
| Сравнить два коммита | `git diff <hash1>..<hash2>` |
| Создать backup ветку | `git branch backup` |
| Найти потерянный коммит | `git reflog` |

---

## 🎓 Примеры реальных ситуаций

### Ситуация 1: Email не работает, нужно откатить
```bash
# Вернуться к версии без email
git reset --hard 789bd4e

# Проверить что все работает
npm run dev

# Если нужно вернуть email обратно
git reflog  # Найти хеш коммита с email
git reset --hard 4ee33c9
```

### Ситуация 2: Нужна промежуточная версия
```bash
# Например, нужна версия с платежами, но без email
git reset --hard 789bd4e
```

### Ситуация 3: Хочу посмотреть старую версию без изменений
```bash
# Создать временную ветку на старом коммите
git checkout -b temp-old-version 5c464c5

# Посмотреть, протестировать

# Вернуться обратно
git checkout master
git branch -D temp-old-version
```

---

## 🔍 Визуализация истории

Для лучшего понимания можно использовать GUI:
- **VS Code:** Расширение "Git Graph"
- **GitKraken:** Визуальный клиент (бесплатный)
- **GitHub Desktop:** Простой интерфейс

Или в терминале:
```bash
# Красивый ASCII граф
git log --all --decorate --oneline --graph
```

---

**Последнее обновление:** 2024-12-24  
**Текущий коммит:** f7ee7ca

