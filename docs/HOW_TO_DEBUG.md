# Как отладить апгрейд подписки

## 1. Логи сервера (Next.js)

### Где искать:
Сервер запущен на порту 3000. Логи могут быть в:
- **Другом окне терминала** (где вы запускали `npm run dev`)
- **Встроенном терминале VS Code/Cursor** (если запускали оттуда)
- **Отдельном окне PowerShell/CMD**

### Что делать:
1. Найдите окно, где запущен `npm run dev`
2. Или запустите новый терминал и выполните:
   ```bash
   npm run dev
   ```
3. Логи будут появляться там при обработке платежей

### Что искать в логах:
```
[ProcessPayment] ========== UPGRADE PROCESSING ==========
[ConversionCalc] Remaining days: 30
[ConversionCalc] Converted days: 24
[ProcessPayment] TOTAL days from NOW: 54
[ProcessPayment] Calculated expiration: [дата]
```

---

## 2. Логи в Chrome DevTools (Network)

### Шаг 1: Откройте DevTools
- Нажмите `F12` или `Ctrl+Shift+I`
- Перейдите на вкладку **Network**

### Шаг 2: Очистите и отфильтруйте
1. Нажмите кнопку **Clear** (🚫) чтобы очистить список
2. В поле фильтра введите: `payments`
3. Или выберите фильтр: **XHR** или **Fetch**

### Шаг 3: Сделайте апгрейд
1. Выполните апгрейд в браузере
2. В Network появятся запросы:
   - `/api/payments/create` - создание платежа
   - `/api/payments/webhook` - обработка webhook (если есть)

### Шаг 4: Посмотрите детали запроса
1. Кликните на запрос `/api/payments/create`
2. Перейдите на вкладку **Response** - там ответ сервера
3. Перейдите на вкладку **Payload** - там данные запроса

### Шаг 5: Проверьте ответ
В Response должно быть:
```json
{
  "success": true,
  "paymentId": "...",
  "confirmationUrl": "..."
}
```

---

## 3. Логи в Console (браузер)

### Где:
- DevTools → вкладка **Console**

### Что искать:
- Ошибки JavaScript (красным цветом)
- Логи с префиксом `[Payment]` или `[Conversion]`

---

## 4. Проверка в Supabase

### После апгрейда проверьте:
1. Откройте Supabase Dashboard
2. Таблица `profiles` → найдите свой профиль
3. Проверьте поле `subscription_expires_at`
4. Должна быть дата через 54 дня (для Basic 30 → Pro 30)

---

## Быстрая проверка

### Если видите в логах:
- `TOTAL days from NOW: 30` вместо `54` → конвертация не работает
- `Converted days: 0` → проблема в расчете конвертации
- `Failed to calculate conversion` → ошибка в функции конвертации

### Что делать:
1. Скопируйте логи из терминала
2. Скопируйте ответ из Network → Response
3. Проверьте данные в Supabase
4. Отправьте мне для анализа

