# 🧪 Тест: Админ-панель пользователей

## Запуск
```bash
npm run dev
# Откройте http://localhost:3000
```

## Шаги тестирования

### 1. Вход как админ
- Войдите с учётной записью админа
- Перейдите на `/admin`
- Нажмите кнопку **"Пользователи"**

### 2. Страница `/admin/users`
✅ Должна отобразиться страница со статистикой:
- Всего пользователей
- Количество админов
- Активных подписок
- Pro+ пользователей

✅ Должна отобразиться таблица с колонками:
- Email
- Роль (badge: админ/пользователь)
- Тариф (badge: free/basic/pro/elite)
- Статус (badge: активна/неактивна/отменена)
- Истекает (дата)
- Действия (кнопка "Редактировать")

### 3. Фильтры
✅ Проверьте фильтрацию:
- По роли (все, пользователь, админ)
- По тарифу (все, free, basic, pro, elite)
- По статусу (все, активна, неактивна, отменена)

✅ Проверьте поиск:
- Введите часть email
- Нажмите "Применить"
- Должны отобразиться только подходящие пользователи

### 4. Редактирование пользователя
✅ Нажмите "Редактировать" у любого пользователя
✅ Должен открыться диалог с полями:
- Email (только чтение)
- Роль (dropdown: Пользователь/Администратор)
- Тариф (dropdown: free/basic/pro/elite)
- Статус (dropdown: Активна/Неактивна/Отменена)
- Действует до (date picker)

✅ Измените значения и нажмите "Сохранить"
✅ Страница должна перезагрузиться
✅ Изменения должны отобразиться в таблице

### 5. Проверка в Supabase
- Откройте Supabase Dashboard
- Перейдите в Table Editor → profiles
- Найдите отредактированного пользователя
- Убедитесь, что изменения сохранены

### 6. Проверка доступа
✅ Выйдите и войдите как обычный пользователь
✅ Попробуйте открыть `/admin/users`
✅ Должен произойти редирект на `/dashboard`

## Ожидаемый результат

✅ Всё работает корректно
✅ Dropdown'ы отображаются правильно
✅ Изменения сохраняются в БД
✅ RLS работает (обычные пользователи не имеют доступа)

## Возможные проблемы

### Ошибка: "Доступ запрещён"
**Решение:** Убедитесь, что в Supabase есть политика:
```sql
CREATE POLICY "Admins can view all profiles"
  ON profiles FOR SELECT
  TO authenticated
  USING (
    EXISTS (
      SELECT 1 FROM profiles p
      WHERE p.id = auth.uid() AND p.role = 'admin'
    )
  );
```

### Не отображаются пользователи
**Проверьте:**
1. Есть ли пользователи в таблице `profiles`
2. Ваш профиль имеет `role = 'admin'`
3. RLS политики активны

---

**Дата:** 23 декабря 2025

