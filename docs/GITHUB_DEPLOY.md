# 🚀 Деплой через GitHub Actions

Автоматический деплой на Vercel при пуше в ветку `main`.

## 📋 Настройка

### 1. Получите Vercel токены

1. Перейдите на [vercel.com/account/tokens](https://vercel.com/account/tokens)
2. Создайте новый токен (Token)
3. Скопируйте токен

### 2. Получите Vercel Project ID и Org ID

1. Перейдите в настройки проекта на Vercel
2. Откройте **Settings → General**
3. Найдите:
   - **Project ID** (в разделе "Project ID")
   - **Team ID** или **Org ID** (в разделе "Team" или "Organization")

### 3. Добавьте секреты в GitHub

1. Перейдите в репозиторий на GitHub
2. Откройте **Settings → Secrets and variables → Actions**
3. Нажмите **New repository secret**
4. Добавьте следующие секреты:

#### Обязательные секреты:

- **`VERCEL_TOKEN`** - токен из шага 1
- **`VERCEL_ORG_ID`** - ID организации/команды из шага 2
- **`VERCEL_PROJECT_ID`** - ID проекта из шага 2

#### Для сборки (опционально, если нужна проверка линтера):

- **`NEXT_PUBLIC_SUPABASE_URL`** - URL вашего Supabase проекта
- **`NEXT_PUBLIC_SUPABASE_ANON_KEY`** - Anon key из Supabase

> **Примечание:** Эти переменные нужны только для проверки сборки. Основные переменные окружения должны быть настроены в Vercel Dashboard.

## 🔄 Как работает деплой

1. При пуше в ветку `main` автоматически запускается workflow
2. Устанавливаются зависимости
3. Запускается линтер
4. Собирается проект
5. Деплоится на Vercel

## 📝 Проверка деплоя

После пуша в `main`:

1. Перейдите в **Actions** на GitHub
2. Выберите последний workflow run
3. Проверьте, что все шаги выполнены успешно
4. Проверьте деплой на Vercel Dashboard

## ⚠️ Важно

- Все переменные окружения должны быть настроены в **Vercel Dashboard → Settings → Environment Variables**
- GitHub Actions использует только токены для деплоя
- Секреты в GitHub нужны только для доступа к Vercel API

