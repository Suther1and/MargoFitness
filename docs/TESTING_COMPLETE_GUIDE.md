# 🧪 Полное руководство по тестированию

## Подготовка к тестированию

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте `.env.local` со следующими переменными:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# ЮKassa (mock для разработки)
YOOKASSA_SHOP_ID=mock_shop_id
YOOKASSA_SECRET_KEY=mock_secret_key
NEXT_PUBLIC_YOOKASSA_RETURN_URL=http://localhost:3000/dashboard
YOOKASSA_WEBHOOK_SECRET=mock_webhook_secret

# Cron Job
CRON_SECRET=dev_secret_key_12345

# Email (Resend) - ОБЯЗАТЕЛЬНО для тестирования email
RESEND_API_KEY=re_your_real_api_key
EMAIL_FROM=MargoFitness <onboarding@resend.dev>

# App URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 3. Запуск dev сервера

```bash
npm run dev
```

---

## Тестовый сценарий 1: Email уведомления

### 1.1 Приветственное письмо при регистрации

**Шаги:**
1. Откройте `http://localhost:3000/auth/signup`
2. Зарегистрируйте нового пользователя с ВАШИМ email (важно!)
3. Проверьте папку входящих (и спам)

**Ожидаемый результат:**
- ✅ Приветственное письмо пришло в течение 5 секунд
- ✅ Дизайн письма выглядит красиво
- ✅ Кнопка "Перейти в личный кабинет" работает
- ✅ Имя пользователя отображается правильно

### 1.2 Тест через API endpoint

```bash
# Замените your@email.com на ваш реальный email
http://localhost:3000/api/test-email?type=welcome&to=your@email.com
http://localhost:3000/api/test-email?type=payment&to=your@email.com
http://localhost:3000/api/test-email?type=upgrade&to=your@email.com
```

**Ожидаемый результат:**
- ✅ API возвращает `{ "success": true }`
- ✅ Письма приходят для всех типов
- ✅ В консоли сервера видны логи `[Email] ... sent successfully`

### 1.3 Email при оплате подписки

**Шаги:**
1. Войдите в систему
2. Перейдите в `/pricing`
3. Выберите тариф и оплатите (mock оплата)
4. Проверьте email

**Ожидаемый результат:**
- ✅ Письмо об успешной оплате пришло
- ✅ Детали подписки корректны (план, сумма, срок)

---

## Тестовый сценарий 2: Апгрейд подписки

### 2.1 Ограничения при покупке

**Шаги:**
1. Имея активную подписку Basic
2. Попробуйте купить Basic через `/pricing`

**Ожидаемый результат:**
- ✅ Система блокирует покупку того же уровня
- ✅ Показывается сообщение с предложением апгрейда

### 2.2 UI апгрейда

**Шаги:**
1. Откройте Dashboard (`/dashboard`)
2. Прокрутите до секции "Управление подпиской"
3. Найдите блок "🚀 Хотите больше возможностей?"
4. Нажмите "Улучшить план"

**Ожидаемый результат:**
- ✅ Открывается диалог апгрейда
- ✅ Показываются только более высокие тарифы
- ✅ Видна информация о текущей подписке
- ✅ Отображается расчет бонусных дней

### 2.3 Выполнение апгрейда

**Шаги:**
1. В диалоге апгрейда выберите новый тариф
2. Нажмите кнопку "Апгрейд за X ₽"
3. Дождитесь обработки

**Ожидаемый результат:**
- ✅ Апгрейд выполнен успешно
- ✅ Подписка обновлена с бонусными днями
- ✅ Email уведомление об апгрейде пришло
- ✅ В Dashboard отображается новый тариф

### 2.4 Проверка конвертации дней

**Пример расчета:**
- Текущая: Basic 3 месяца (11397₽), осталось 30 дней
- Новая: Pro 6 месяцев (24294₽)
- Остаток Basic: 30 × (11397/90) ≈ 3799₽
- Конвертация: 3799 / (24294/180) ≈ 28 дней Pro
- **Итого: 180 + 28 = 208 дней Pro подписки**

**Проверьте:**
- ✅ Расчет корректный
- ✅ Дата окончания подписки учитывает бонусные дни

---

## Тестовый сценарий 3: Аналитика в админке

### 3.1 Доступ к аналитике

**Шаги:**
1. Войдите как администратор
2. Откройте `/admin`
3. Нажмите кнопку "Аналитика"

**Ожидаемый результат:**
- ✅ Страница аналитики открывается
- ✅ Данные загружаются и отображаются

### 3.2 Проверка метрик

**Что проверить:**
- ✅ **Общая выручка:** Сумма всех успешных транзакций
- ✅ **Активные подписки:** Количество пользователей с active status
- ✅ **Средний чек:** Выручка / количество транзакций
- ✅ **Конверсия:** (Платные подписки / Всего пользователей) × 100%

### 3.3 Выручка по тарифам

**Проверьте:**
- ✅ Прогресс-бары отображаются корректно
- ✅ Суммы соответствуют транзакциям
- ✅ Процентное соотношение правильное

### 3.4 Подписчики по уровням

**Проверьте:**
- ✅ Количество на каждом тарифе верное
- ✅ Цвета индикаторов соответствуют тарифам
- ✅ Сумма всех тарифов = общему количеству

### 3.5 Последние транзакции

**Проверьте:**
- ✅ Отображаются последние 10 транзакций
- ✅ Данные корректны (дата, пользователь, сумма)
- ✅ Статусы отображаются с правильными цветами

---

## Тестовый сценарий 4: Интеграция всех фич

### 4.1 Полный флоу нового пользователя

**Шаги:**
1. Регистрация → Приветственное письмо ✉️
2. Покупка Basic → Email об оплате ✉️
3. Апгрейд до Pro → Email об апгрейде ✉️
4. Проверка аналитики → Все метрики обновлены 📊

**Ожидаемый результат:**
- ✅ Все 3 email пришли
- ✅ Подписка активна с бонусными днями
- ✅ В аналитике видны транзакции
- ✅ Выручка учтена корректно

### 4.2 Проверка автопродления (optional)

**Требует:**
- Настроенный cron job на Vercel
- Или ручной вызов `/api/cron/renew-subscriptions`

**Проверьте:**
- ✅ Подписки с `next_billing_date = сегодня` обрабатываются
- ✅ Платежи создаются через сохраненную карту
- ✅ Email об успешном продлении отправляется

---

## Проверка в production

### Checklist перед деплоем:

#### Email (Resend)
- [ ] Зарегистрирован аккаунт на resend.com
- [ ] Создан production API key
- [ ] Добавлен в Vercel Environment Variables
- [ ] Настроен свой домен (опционально, но рекомендуется)
- [ ] Обновлен `EMAIL_FROM` на свой домен

#### Платежи (ЮКасса)
- [ ] Получены реальные `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`
- [ ] Добавлены в Vercel Environment Variables
- [ ] Настроен webhook URL в ЮКасса dashboard
- [ ] Получен `YOOKASSA_WEBHOOK_SECRET`

#### Общее
- [ ] Удален `/api/test-email/route.ts` (тестовый endpoint)
- [ ] Проверены все переменные окружения в Vercel
- [ ] Протестирован тестовый платеж на production
- [ ] Проверена отправка email на production

---

## Troubleshooting

### Email не приходят

1. **Проверьте API ключ:**
   ```bash
   echo $RESEND_API_KEY  # Должен начинаться с re_
   ```

2. **Проверьте логи сервера:**
   Ищите строки `[Email] Sending ... email to:`

3. **Проверьте Resend dashboard:**
   Все попытки отправки видны в веб-интерфейсе

4. **Проверьте спам:**
   Письма могут попадать в спам на первых порах

### Апгрейд не работает

1. **Проверьте текущую подписку:**
   В БД `profiles` → `subscription_status = 'active'`

2. **Проверьте уровень тарифа:**
   Апгрейд возможен только на более высокий уровень

3. **Проверьте наличие продуктов:**
   `/api/products/by-duration?duration=all` должен возвращать продукты

### Аналитика пустая

1. **Проверьте роль пользователя:**
   В БД `profiles` → `role = 'admin'`

2. **Создайте тестовые транзакции:**
   Купите несколько подписок через mock платежи

3. **Проверьте таблицу транзакций:**
   `payment_transactions` должна содержать записи

---

## Финальная проверка

### Все работает, если:

✅ **Email:**
- Регистрация → письмо пришло
- Оплата → письмо пришло
- Апгрейд → письмо пришло

✅ **Апгрейд:**
- Ограничения работают
- UI апгрейда открывается
- Конвертация дней корректна
- Email уведомление отправлено

✅ **Аналитика:**
- Метрики отображаются
- Выручка подсчитана правильно
- Подписчики по уровням корректны
- Транзакции видны в таблице

✅ **Интеграция:**
- Все компоненты работают вместе
- Данные синхронизированы
- Нет ошибок в консоли

---

## Поддержка

При возникновении проблем:
1. Проверьте логи сервера (`npm run dev`)
2. Проверьте переменные окружения
3. Прочитайте документацию:
   - `EMAIL_SETUP.md`
   - `EMAIL_TESTING.md`
   - `PAYMENT_SYSTEM_README.md`

**Удачного тестирования! 🚀**

