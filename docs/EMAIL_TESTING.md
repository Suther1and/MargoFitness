# 🧪 Тестирование Email уведомлений

## Быстрый тест

### 1. Получите Resend API ключ

1. Зарегистрируйтесь на [resend.com](https://resend.com/)
2. Создайте API ключ в dashboard
3. Добавьте в `.env.local`:

```bash
RESEND_API_KEY=re_ваш_ключ
EMAIL_FROM=MargoFitness <onboarding@resend.dev>
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 2. Перезапустите dev сервер

```bash
npm run dev
```

### 3. Тестируйте через API endpoint

**Важно:** Замените `your@email.com` на ваш реальный email (тот, который указан в Resend аккаунте для тестов)

#### Приветственное письмо
```bash
http://localhost:3000/api/test-email?type=welcome&to=your@email.com
```

#### Успешная оплата
```bash
http://localhost:3000/api/test-email?type=payment&to=your@email.com
```

#### Апгрейд подписки
```bash
http://localhost:3000/api/test-email?type=upgrade&to=your@email.com
```

#### Изменение подписки
```bash
http://localhost:3000/api/test-email?type=change&to=your@email.com
```

### 4. Проверьте результат

- Откройте ссылку в браузере
- Проверьте ответ API (success: true)
- Проверьте email (может прийти в спам)
- Проверьте Resend dashboard для истории

## Тестирование в реальных сценариях

### Регистрация нового пользователя

1. Откройте `/auth/signup`
2. Зарегистрируйтесь с вашим email
3. Проверьте приветственное письмо

### Покупка подписки

1. Войдите в систему
2. Перейдите в `/pricing`
3. Выберите тариф и оплатите (mock оплата)
4. Проверьте письмо об успешной оплате

### Апгрейд подписки

1. Имея активную подписку Basic
2. Выполните апгрейд до Pro через API
3. Проверьте письмо об апгрейде с бонусными днями

## Что проверять в письмах

### ✅ Дизайн
- [ ] Письмо красиво выглядит на desktop
- [ ] Письмо адаптивно на mobile
- [ ] Градиенты отображаются корректно
- [ ] Кнопки кликабельны

### ✅ Контент
- [ ] Имя пользователя отображается правильно
- [ ] Данные подписки корректны (план, сумма, дата)
- [ ] Ссылки ведут на правильные страницы
- [ ] Нет опечаток

### ✅ Функциональность
- [ ] Письмо приходит быстро (обычно < 5 сек)
- [ ] Кнопки в письме работают
- [ ] Ссылки открываются в новой вкладке
- [ ] Email не попадает в спам

## Troubleshooting

### Письмо не пришло

1. Проверьте папку спам
2. Проверьте логи в терминале (должен быть `[Email] ... sent successfully`)
3. Проверьте Resend dashboard → Emails
4. Убедитесь что `RESEND_API_KEY` установлен правильно

### Ошибка 401 Unauthorized

```bash
# Проверьте переменную окружения
echo $RESEND_API_KEY

# Перезапустите dev сервер после добавления
npm run dev
```

### Ошибка 403 Forbidden (recipient not allowed)

На бесплатном плане с тестовым доменом можно отправлять только на:
- Ваш зарегистрированный email в Resend
- Email добавленные в Resend → Settings → Testing

## Мониторинг

### В Resend Dashboard

- **Emails tab:** История всех отправленных писем
- **Status:** delivered, bounced, complained
- **Open rate:** Сколько писем открыли (если включено tracking)

### В логах приложения

Все email операции логируются:
```
[Email] Sending welcome email to: user@example.com
[Email] Welcome email sent successfully
```

## Production чеклист

Перед деплоем:

- [ ] Удалить `app/api/test-email/route.ts`
- [ ] Настроить свой домен в Resend
- [ ] Обновить `EMAIL_FROM` на ваш домен
- [ ] Добавить `RESEND_API_KEY` в Vercel environment variables
- [ ] Протестировать отправку с production

