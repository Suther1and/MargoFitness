# 🔄 Гайд по миграции на ENUM типы

## Что это даст?

После применения этой миграции в Supabase Table Editor:
- ✅ Для `role` появится **dropdown** с вариантами: `user` | `admin`
- ✅ Для `subscription_status` появится **dropdown**: `active` | `inactive` | `canceled`
- ✅ Для `subscription_tier` уже есть dropdown: `free` | `basic` | `pro` | `elite`
- ✅ При наведении на столбцы будут **подсказки**

**Было:**
```
role: [___текстовое поле___] ❌ можно ввести что угодно
```

**Станет:**
```
role: [▼ dropdown: user | admin] ✅ только валидные значения
```

---

## ⚠️ ВАЖНО: Проверка перед применением

### Вариант А: У вас ОДИН тестовый пользователь (вы)
✅ **Можно применять безопасно** - данные автоматически мигрируют

### Вариант Б: У вас УЖЕ ЕСТЬ реальные пользователи
⚠️ **Сначала сделайте бэкап БД!**
1. Supabase Dashboard → Database → Backups
2. Create backup → назовите `before-enum-migration`
3. Затем применяйте скрипт

---

## 🚀 Пошаговая инструкция

### Шаг 1: Откройте Supabase SQL Editor

1. Откройте [Supabase Dashboard](https://supabase.com/dashboard/project/yxzrenwkkntnhmdimhln/sql)
2. Перейдите в **SQL Editor**
3. Нажмите **New Query**

### Шаг 2: Скопируйте SQL скрипт

1. Откройте файл [`database/migrations/004_ENUM_OPTIMIZATION.sql`](../../database/migrations/004_ENUM_OPTIMIZATION.sql)
2. **Скопируйте весь код** (Ctrl+A, Ctrl+C)
3. **Вставьте** в SQL Editor (Ctrl+V)

### Шаг 3: Выполните скрипт

1. Нажмите **Run** (или F5)
2. Дождитесь сообщения: `ENUM_OPTIMIZATION COMPLETE - Check Supabase Table Editor!`
3. Если появились ошибки - смотрите раздел "Troubleshooting" ниже

### Шаг 4: Проверьте результат

1. Откройте **Table Editor** → **profiles**
2. Кликните на любую строку (пользователя)
3. Попробуйте изменить **role**:
   - ✅ Должен появиться dropdown: `user` | `admin`
4. Попробуйте изменить **subscription_status**:
   - ✅ Должен появиться dropdown: `active` | `inactive` | `canceled`
5. Наведите курсор на названия столбцов:
   - ✅ Должны появиться подсказки

---

## 🧪 Тестирование приложения

После применения миграции проверьте, что всё работает:

### 1. Базовые функции
```bash
# Запустите dev сервер (если не запущен)
npm run dev
```

- [ ] Главная страница открывается
- [ ] Регистрация работает (создается профиль с role='user')
- [ ] Логин работает
- [ ] Dashboard показывает правильный tier и status

### 2. Админ функции
- [ ] Админ видит админку
- [ ] Админ может создавать недели/тренировки
- [ ] Админ может редактировать бесплатные материалы

### 3. Подписки
- [ ] Можно изменить tier через админку Supabase
- [ ] Изменения сразу отражаются в приложении
- [ ] Доступ к тренировкам работает корректно

---

## ❌ Troubleshooting

### Ошибка: "type user_role already exists"

**Причина:** Скрипт уже был выполнен ранее.

**Решение:** Пропустите этот скрипт, у вас уже всё настроено!

---

### Ошибка: "column role_new already exists"

**Причина:** Скрипт был выполнен частично и прервался.

**Решение:**
1. Откатите БД из бэкапа
2. Или вручную удалите временные колонки:
```sql
ALTER TABLE profiles DROP COLUMN IF EXISTS role_new;
ALTER TABLE profiles DROP COLUMN IF EXISTS subscription_status_new;
```
3. Выполните скрипт заново

---

### Ошибка: "invalid input value for enum"

**Причина:** В БД есть значения, не соответствующие ENUM.

**Решение:**
1. Проверьте данные:
```sql
SELECT DISTINCT role FROM profiles;
SELECT DISTINCT subscription_status FROM profiles;
```
2. Исправьте неправильные значения:
```sql
-- Если есть что-то кроме 'user' и 'admin'
UPDATE profiles SET role = 'user' WHERE role NOT IN ('user', 'admin');

-- Если есть что-то кроме 'active', 'inactive', 'canceled'
UPDATE profiles SET subscription_status = 'inactive' 
WHERE subscription_status NOT IN ('active', 'inactive', 'canceled');
```
3. Выполните скрипт заново

---

## 🔄 Откат (если что-то пошло не так)

### Вариант 1: Через бэкап
1. Supabase Dashboard → Database → Backups
2. Найдите бэкап `before-enum-migration`
3. Нажмите **Restore**

### Вариант 2: Вручную (если нет бэкапа)
```sql
-- Вернуть к text типам
DROP TYPE IF EXISTS user_role CASCADE;
DROP TYPE IF EXISTS subscription_status_enum CASCADE;

-- Пересоздать таблицу profiles с text
-- (используйте 001_CLEAN_SETUP.sql)
```

---

## 📝 После успешной миграции

### 1. Обновите TypeScript типы (необязательно)

Технически типы в TypeScript уже правильные, но для точности:

```bash
# Сгенерируйте новые типы (если хотите)
npx supabase gen types typescript --project-id yxzrenwkkntnhmdimhln > types/supabase.ts
```

### 2. Закоммитьте изменения

```bash
git add .
git commit -m "Этап 2: ENUM оптимизация для Supabase UI"
```

---

## ✅ Готово!

Теперь работать с данными в Supabase Table Editor стало намного удобнее! 🎉

**Следующий этап:** Админ-панель для управления пользователями

