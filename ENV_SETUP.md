# Настройка переменных окружения для ЮKassa

## Файл `.env.local`

Создайте файл `.env.local` в корне проекта и добавьте следующие переменные:

```env
# ============================================
# Supabase Configuration
# ============================================
# Эти переменные у вас уже есть
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================
# ЮKassa (Яндекс.Касса) - ДОБАВЬТЕ ЭТИ НОВЫЕ
# ============================================

# ID магазина (shopId)
YOOKASSA_SHOP_ID=your_shop_id

# Секретный ключ (secretKey)
YOOKASSA_SECRET_KEY=your_secret_key

# URL возврата после оплаты
NEXT_PUBLIC_YOOKASSA_RETURN_URL=http://localhost:3000/payment/success

# Секрет для webhook (любая случайная строка)
YOOKASSA_WEBHOOK_SECRET=any_random_string_here

# ============================================
# Cron Jobs - ДОБАВЬТЕ ЭТУ НОВУЮ
# ============================================

# Секрет для защиты cron endpoints
CRON_SECRET=another_random_string_here
```

## Как получить ключи ЮKassa

### Вариант 1: Тестовые ключи (для разработки)

1. Перейдите на https://yookassa.ru
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел **"Интеграция"** → **"API-ключи"**
4. Переключитесь на **"Тестовый режим"**
5. Скопируйте:
   - `shopId` → вставьте в `YOOKASSA_SHOP_ID`
   - `Секретный ключ` → вставьте в `YOOKASSA_SECRET_KEY`

### Вариант 2: Боевые ключи (для production)

Для production вам нужно:
1. Пройти модерацию в ЮKassa
2. Подключить юридическое лицо
3. Получить боевые ключи

**Пока используйте тестовые ключи!**

## Генерация случайных строк

Для `YOOKASSA_WEBHOOK_SECRET` и `CRON_SECRET` используйте любые случайные строки.

Можно сгенерировать через терминал:

```bash
# В PowerShell (Windows)
[Convert]::ToBase64String([System.Security.Cryptography.RandomNumberGenerator]::GetBytes(32))

# Или просто придумайте любую строку
# Например: my_super_secret_webhook_key_12345
```

## Тестовые карты для проверки

После настройки ЮKassa используйте эти карты для тестирования:

| Карта | Результат |
|-------|-----------|
| `5555 5555 5555 4477` | ✅ Успешная оплата |
| `5555 5555 5555 5599` | ❌ Отклонена |
| `5555 5555 5555 5585` | 🔐 3DS (код: 123456) |

**Срок:** любой в будущем (например, 12/25)  
**CVV:** любой трехзначный (например, 123)

## Проверка настройки

После добавления переменных в `.env.local`:

1. Перезапустите dev сервер
2. Проверьте что переменные загружаются:

```bash
# Остановите сервер (Ctrl+C)
npm run dev
```

## ⚠️ Важно!

- **НЕ коммитьте** `.env.local` в git (он уже в `.gitignore`)
- Используйте **тестовые ключи** для разработки
- **Боевые ключи** добавляйте только в production окружение

