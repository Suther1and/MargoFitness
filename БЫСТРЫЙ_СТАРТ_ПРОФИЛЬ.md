# 🚀 БЫСТРЫЙ СТАРТ - Профиль пользователя

## ✅ ЧТО УЖЕ СДЕЛАНО (код готов):

- ✅ База данных: поля `phone` и `profile_completed_at`
- ✅ Компонент круглого аватара с заглушкой (инициалы)
- ✅ Модальное окно редактирования профиля
- ✅ Загрузка аватара в Supabase Storage
- ✅ Маска для телефона +7 (999) 999-99-99
- ✅ Автоматический показ модалки при первом входе
- ✅ Обновленный дизайн личного кабинета
- ✅ Кнопка "Редактировать" профиль
- ✅ Валидация файлов (тип, размер)
- ✅ Автозаполнение из OAuth

## 📋 ЧТО НУЖНО СДЕЛАТЬ ВРУЧНУЮ (2 шага):

### ШАГ 1: Выполнить миграцию БД (1 минута)

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте весь код из файла:
   ```
   database/migrations/006_ADD_PHONE_AND_PROFILE_COMPLETION.sql
   ```
4. Вставьте в SQL Editor и нажмите **RUN**
5. Должно появиться сообщение с результатом ✅

### ШАГ 2: Создать Storage Bucket (2 минуты)

#### 2.1 Создание bucket:
1. В Supabase Dashboard перейдите в **Storage**
2. Нажмите **New bucket**
3. Заполните:
   - **Name**: `avatars`
   - **Public bucket**: ✅ ВКЛ
   - **Allowed MIME types**: `image/jpeg, image/png, image/webp`
   - **File size limit**: `5242880` (5MB в байтах)
4. Нажмите **Create bucket**

#### 2.2 Настройка RLS политик:
1. Перейдите в **SQL Editor**
2. Вставьте и выполните этот код:

```sql
-- Пользователи могут загружать только свои аватары
CREATE POLICY "Users can upload their own avatar"
ON storage.objects FOR INSERT
TO authenticated
WITH CHECK (
  bucket_id = 'avatars' 
  AND auth.uid()::text = (storage.foldername(name))[1]
);

-- Пользователи могут обновлять только свои аватары
CREATE POLICY "Users can update their own avatar"
ON storage.objects FOR UPDATE
TO authenticated
USING (
  bucket_id = 'avatars' 
  AND auth.uid()::text = (storage.foldername(name))[1]
);

-- Пользователи могут удалять только свои аватары
CREATE POLICY "Users can delete their own avatar"
ON storage.objects FOR DELETE
TO authenticated
USING (
  bucket_id = 'avatars' 
  AND auth.uid()::text = (storage.foldername(name))[1]
);

-- Все могут просматривать аватары
CREATE POLICY "Anyone can view avatars"
ON storage.objects FOR SELECT
TO public
USING (bucket_id = 'avatars');
```

3. Нажмите **RUN** ✅

### ШАГ 3: Запустить и протестировать

```bash
npm run dev
```

Откройте http://localhost:3000 и войдите в личный кабинет!

---

## 🎉 ВСЁ ГОТОВО!

### Что теперь работает:

✅ При первом входе автоматически откроется модальное окно  
✅ Данные из Google (имя, аватар) уже заполнены  
✅ Можно добавить телефон с красивой маской  
✅ Можно загрузить свой аватар (или оставить из Google)  
✅ Можно пропустить и заполнить позже  
✅ В личном кабинете красивая карточка профиля  
✅ Кнопка "Редактировать" открывает ту же форму  
✅ Круглый аватар с инициалами если нет фото  

---

## 🐛 Если что-то не работает:

### Проблема: Модалка не появляется при первом входе
**Решение**: Проверьте что миграция выполнена и поле `profile_completed_at` равно `NULL`

### Проблема: Ошибка при загрузке аватара
**Решение**: 
1. Проверьте что bucket `avatars` создан
2. Проверьте что RLS политики применены
3. Проверьте что bucket публичный (Public bucket = ON)

### Проблема: Аватар не отображается
**Решение**: Откройте DevTools → Network и проверьте URL аватара. Он должен быть вида:
```
https://[ваш-проект].supabase.co/storage/v1/object/public/avatars/...
```

---

**Вопросы? Проблемы? Пишите!** 💬

