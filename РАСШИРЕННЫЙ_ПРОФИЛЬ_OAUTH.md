# ✅ Расширенный профиль OAuth - ГОТОВО!

## 🎯 Что добавлено

Теперь при авторизации через Google мы собираем:

1. ✅ **Email** (как и раньше)
2. ✅ **Полное имя** (full_name) - из Google Profile
3. ✅ **Аватар** (avatar_url) - фотография профиля Google

---

## 📋 Выполненные изменения

### 1. База данных

**Файл:** `database/ADD_USER_PROFILE_FIELDS.sql`

Добавлены новые колонки в таблицу `profiles`:
```sql
- full_name text
- avatar_url text
```

### 2. Триггер обновлен

Функция `handle_new_user()` теперь автоматически извлекает и сохраняет:
- Имя из `raw_user_meta_data->>'full_name'` или `->>'name'`
- Аватар из `raw_user_meta_data->>'avatar_url'` или `->>'picture'`

### 3. TypeScript типы

**Файл:** `types/supabase.ts`

Добавлены поля в интерфейс Profile:
```typescript
full_name: string | null
avatar_url: string | null
```

### 4. UI обновлен

#### Dashboard (`app/dashboard/page.tsx`)
- ✅ Показывает аватар пользователя (если есть)
- ✅ Показывает полное имя вместо email
- ✅ В секции "Информация для разработки" показывает все новые поля

#### Navbar (`components/navbar.tsx`)
- ✅ Показывает круглый аватар в навигации
- ✅ Кликабелен - ведет в личный кабинет

#### Callback Route (`app/auth/callback/route.ts`)
- ✅ Извлекает metadata из Google OAuth
- ✅ Сохраняет имя и аватар при создании профиля

---

## 🚀 Установка

### ШАГ 1: Выполните SQL миграцию

1. Откройте **Supabase Dashboard** → **SQL Editor**
2. Откройте файл `database/ADD_USER_PROFILE_FIELDS.sql`
3. Скопируйте весь код
4. Вставьте в SQL Editor
5. Нажмите **RUN**

Вы увидите результат:
```
✅ Success
```

И таблицу с существующими пользователями, где будут показаны их имена и аватары.

### ШАГ 2: Перезапустите dev сервер

```bash
# Остановите текущий сервер (Ctrl+C в терминале)
npm run dev
```

---

## 🧪 Тестирование

### Вариант 1: Новый пользователь

1. Откройте **инкогнито** (Ctrl+Shift+N)
2. Перейдите на `http://localhost:3000/auth/login`
3. Нажмите **Google**
4. Авторизуйтесь **новым** Google аккаунтом
5. Проверьте `/dashboard` - должны увидеть:
   - ✅ Ваше имя из Google
   - ✅ Ваш аватар из Google

### Вариант 2: Существующий пользователь

Если вы уже авторизовывались раньше:

1. SQL миграция уже обновила ваш профиль из `auth.users.raw_user_meta_data`
2. Просто перейдите на `/dashboard`
3. Должны увидеть свое имя и аватар

Если не видите - выйдите и войдите снова через Google.

---

## 🔍 Проверка в Supabase

### 1. Откройте Table Editor → profiles

Вы должны увидеть новые колонки:
- `full_name` - имя пользователя
- `avatar_url` - URL аватара

### 2. Проверьте данные

```sql
SELECT 
  email,
  full_name,
  avatar_url,
  created_at
FROM profiles
ORDER BY created_at DESC;
```

Должны быть заполнены имена и ссылки на аватары!

---

## 📊 Какие данные получаем от Google?

Google OAuth предоставляет следующие данные в `user_metadata`:

```json
{
  "email": "user@gmail.com",
  "name": "John Doe",
  "full_name": "John Doe", 
  "avatar_url": "https://lh3.googleusercontent.com/...",
  "picture": "https://lh3.googleusercontent.com/...",
  "provider_id": "123456789",
  "sub": "123456789"
}
```

**Мы используем:**
- ✅ `full_name` или `name` → сохраняем в `profiles.full_name`
- ✅ `avatar_url` или `picture` → сохраняем в `profiles.avatar_url`

---

## 🎨 Как это выглядит

### Dashboard
```
┌─────────────────────────────────────────┐
│  🖼️  Личный кабинет                     │
│      Добро пожаловать, John Doe!        │
│                                         │
│  📊 Статистика карточки...              │
└─────────────────────────────────────────┘
```

### Navbar
```
┌──────────────────────────────────────────────┐
│ 💪 MargoFitness  [Главная] [Тарифы] 🖼️ [Выход] │
└──────────────────────────────────────────────┘
```

---

## 🔧 Дополнительные настройки

### Хотите собирать еще больше данных?

Google OAuth может предоставить (с дополнительными разрешениями):
- Дату рождения
- Пол
- Телефон
- Адрес

**Как добавить:**

1. В Google Cloud Console добавьте scopes
2. В Supabase OAuth настройках добавьте scopes
3. Обновите триггер для сохранения новых полей

---

## ✅ Что проверить после установки

```
[ ] SQL миграция выполнена успешно
[ ] В Table Editor → profiles есть колонки full_name и avatar_url
[ ] Dev сервер перезапущен
[ ] При входе через Google сохраняется имя
[ ] При входе через Google сохраняется аватар
[ ] Dashboard показывает имя и аватар
[ ] Navbar показывает круглый аватар
[ ] В консоли браузера есть логи [Callback] User metadata
```

---

## 📸 Скриншоты для проверки

### 1. Supabase Table Editor
Должны быть видны колонки `full_name` и `avatar_url` с данными

### 2. Dashboard
Должны быть видны:
- Круглый аватар вверху слева
- Имя пользователя в приветствии
- В секции "Информация для разработки":
  - `Full Name: John Doe`
  - `Avatar: ✅ Есть`

### 3. Navbar
Должен быть виден маленький круглый аватар справа

---

## 🐛 Если что-то не работает

### Аватар не отображается

1. Проверьте в Supabase: `SELECT avatar_url FROM profiles WHERE id = 'ваш-id'`
2. Скопируйте URL и откройте в браузере - открывается ли картинка?
3. Если нет - возможно Google заблокировал внешние запросы

**Решение:** Скачать и загрузить аватар в Supabase Storage.

### Имя не отображается

1. Проверьте в Supabase: `SELECT full_name FROM profiles WHERE id = 'ваш-id'`
2. Если NULL - проверьте логи в терминале: `[Callback] User metadata`
3. Google может не предоставлять имя, если оно не заполнено в профиле

---

## 🎉 Готово!

Теперь ваше приложение собирает расширенную информацию о пользователях через OAuth!

**Следующие шаги:**
- ✅ Можно добавить редактирование профиля (имени и аватара)
- ✅ Можно добавить загрузку кастомного аватара
- ✅ Можно показывать аватары в других местах (комментарии, отзывы и т.д.)

---

**Дата:** 23.12.2025  
**Статус:** ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ

