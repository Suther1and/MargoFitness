# 🔧 Исправление ошибки Supabase

## Проблема

Была ошибка: **"You're importing a component that needs 'next/headers'. That only works in a Server Component"**

## Причина

Файл `lib/supabase.ts` содержал импорт `next/headers`, который может использоваться только на сервере. Но этот файл импортировался и в клиентских компонентах, что вызывало ошибку.

## Решение

Разделили клиентский и серверный код Supabase на два отдельных файла:

### Структура до исправления:
```
lib/
└── supabase.ts (❌ содержал и клиентский, и серверный код)
```

### Структура после исправления:
```
lib/
└── supabase/
    ├── client.ts (✅ только для "use client" компонентов)
    └── server.ts (✅ только для Server Components)
```

## Использование

### ✅ В клиентских компонентах:
```typescript
"use client"
import { createClient } from "@/lib/supabase/client"

export function MyComponent() {
  const supabase = createClient()
  // ...
}
```

### ✅ В серверных компонентах:
```typescript
import { createClient } from "@/lib/supabase/server"

export default async function MyPage() {
  const supabase = await createClient()
  // ...
}
```

## Обновленные файлы

- ✅ `lib/supabase/client.ts` - создан
- ✅ `lib/supabase/server.ts` - создан
- ✅ `lib/supabase.ts` - удален (старый файл)
- ✅ `app/examples/page.tsx` - обновлен импорт
- ✅ `components/supabase-client-example.tsx` - обновлен импорт
- ✅ `middleware.ts` - не требует изменений (использует напрямую @supabase/ssr)
- ✅ Документация обновлена (QUICKSTART.md, SUPABASE_SETUP.md)

## Проверка

Теперь страница http://localhost:3000/examples должна работать без ошибок!

Next.js автоматически перекомпилирует файлы. Просто обновите страницу в браузере.

