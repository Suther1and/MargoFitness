# Миграция 029: Удаление нереализованных достижений

## Описание
Удаляет 3 достижения, которые требуют сложной логики и не будут реализованы в ближайшее время.

## Удаляемые достижения

### 1. 🏅 Стабильность (weight_maintain)
- **Категория**: weight
- **Описание**: Поддерживать целевой вес 14 дней
- **Причина удаления**: Требует сложной логики сравнения текущего веса с целевым каждый день + проверку отклонения

### 2. ✨ Идеальный день (perfect_day)
- **Категория**: consistency
- **Описание**: Выполнить все цели за день
- **Причина удаления**: Не определено что такое "все цели" (вода? шаги? сон? привычки? всё вместе?)

### 3. 🎯 Перфекционист (perfect_streak)
- **Категория**: consistency
- **Описание**: Выполнить все цели 7 дней подряд
- **Причина удаления**: Та же проблема + проверка серии

## Что делает миграция

1. Удаляет достижения из таблицы `achievements`
2. Удаляет связанные записи из `user_achievements` (если кто-то успел их получить)
3. Выводит итоговую статистику достижений по категориям

## Как применить

```bash
# Через Supabase Dashboard
1. Зайти в SQL Editor
2. Скопировать содержимое 029_REMOVE_UNREALIZED_ACHIEVEMENTS.sql
3. Выполнить

# Или через CLI
supabase db push
```

## После применения

**Итого достижений останется: ~24 (из 27)**

Все оставшиеся достижения полностью реализованы и работают:
- ✅ Метрики: 11 достижений
- ✅ Привычки: 5 достижений  
- ✅ Вес: 3 достижения
- ✅ Серии: 1 достижение
- ✅ Регулярность: 4 достижения

## Откат

Если нужно вернуть удалённые достижения:

```sql
-- Вставить обратно из файла 022_ADD_ACHIEVEMENTS_SYSTEM.sql
-- строки 225, 244-245
```

## Связанные файлы
- `lib/actions/achievements.ts` - код проверки (эти типы не обрабатываются)
- `022_ADD_ACHIEVEMENTS_SYSTEM.sql` - оригинальная миграция с этими достижениями
