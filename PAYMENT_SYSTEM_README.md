# 💳 Платежная система MargoFitness

## 🎉 Что реализовано

Полнофункциональная платежная система с поддержкой:
- ✅ Интеграция с ЮKassa (Яндекс.Касса)
- ✅ Рекуррентные платежи (автопродление)
- ✅ 3 уровня подписки (Basic, Pro, Elite)
- ✅ 4 периода подписки (1, 3, 6, 12 месяцев)
- ✅ Автоматические скидки (5%, 10%, 15%)
- ✅ Привязка карт для автопродления
- ✅ Управление подпиской из личного кабинета
- ✅ Админ-панель для управления подписками
- ✅ Cron job для автоматического продления
- ✅ Mock режим для разработки без реальных платежей
- ✅ Email уведомления (Resend) - приветствие, оплата, апгрейд

---

## 📁 Структура проекта

### Миграции БД
```
database/migrations/
├── 008_ADD_YOOKASSA_INTEGRATION.sql  # Основная миграция
├── INSERT_PRODUCTS_ONLY.sql          # Добавление продуктов
├── README_008.md                     # Инструкция по миграции
└── FIX_PRODUCTS_RLS.sql              # Исправление RLS политик
```

### API Routes
```
app/api/
├── payments/
│   ├── create/route.ts              # Создание платежа
│   ├── webhook/route.ts             # Обработка уведомлений от ЮKassa
│   ├── toggle-auto-renew/route.ts   # Включение/отключение автопродления
│   ├── cancel-full/route.ts         # Полная отмена подписки
│   ├── upgrade/route.ts             # Апгрейд подписки
│   ├── cancel-subscription/route.ts # Отмена подписки
│   └── transactions/route.ts        # История транзакций
├── products/
│   └── by-duration/route.ts         # Получение продуктов по периоду
├── cron/
│   └── renew-subscriptions/route.ts # Автопродление (cron job)
└── debug/
    └── products/route.ts            # Отладочный endpoint
```

### Сервисы
```
lib/services/
├── yookassa.ts              # Клиент для работы с API ЮKassa
├── subscription-manager.ts  # Менеджер подписок (логика обработки)
└── email.ts                 # Email сервис (Resend) с шаблонами
```

### UI Компоненты
```
app/
├── pricing/
│   ├── page.tsx             # Страница выбора тарифа (server)
│   └── pricing-client.tsx   # Клиентский компонент с периодами
├── payment/[productId]/
│   ├── page.tsx             # Страница оплаты
│   └── mock-payment-widget.tsx  # Mock виджет оплаты
├── dashboard/
│   ├── page.tsx             # Личный кабинет
│   └── subscription-manager.tsx # Управление подпиской
└── admin/users/
    ├── page.tsx             # Список пользователей
    └── cancel-subscription-button.tsx # Кнопка отмены для админа
```

### UI Kit
```
components/ui/
├── switch.tsx   # Переключатель (для автопродления)
└── checkbox.tsx # Чекбокс (для сохранения карты)
```

---

## 🔄 Флоу работы

### 1. Покупка подписки

```
Пользователь → /pricing
              ↓
    Выбор периода (1/3/6/12 мес)
              ↓
    Выбор тарифа (Basic/Pro/Elite)
              ↓
    /payment/[productId]
              ↓
    Ввод данных карты (mock)
              ↓
    POST /api/payments/create
              ↓
    Платеж создан → paymentId
              ↓
    Имитация успешной оплаты
              ↓
    POST /api/payments/webhook
              ↓
    Подписка активирована ✅
              ↓
    Redirect → /dashboard
```

### 2. Автопродление

```
Cron Job (ежедневно 02:00 UTC)
              ↓
GET /api/cron/renew-subscriptions
              ↓
    Поиск подписок для продления:
    - next_billing_date = сегодня
    - auto_renew_enabled = true
    - payment_method_id не пустой
              ↓
    Для каждой подписки:
              ↓
    autoRenewSubscription(userId)
              ↓
    Создание рекуррентного платежа
              ↓
    Обновление дат подписки
              ↓
    Логирование транзакции
              ↓
    Подписка продлена ✅
```

### 3. Неудачное продление

```
1-я попытка провалилась
              ↓
failed_payment_attempts = 1
              ↓
    Retry на следующий день
              ↓
2-я попытка провалилась
              ↓
failed_payment_attempts = 2
              ↓
    Downgrade на Free тариф
              ↓
subscription_tier = 'free'
subscription_status = 'inactive'
```

---

## 📊 База данных

### Новые поля в `profiles`

| Поле | Тип | Описание |
|------|-----|----------|
| `payment_method_id` | TEXT | ID сохраненной карты |
| `auto_renew_enabled` | BOOLEAN | Включено ли автопродление |
| `subscription_duration_months` | INTEGER | Длительность подписки |
| `next_billing_date` | TIMESTAMP | Дата следующего списания |
| `failed_payment_attempts` | INTEGER | Количество неудачных попыток |
| `last_payment_date` | TIMESTAMP | Дата последнего платежа |

### Новая таблица `payment_transactions`

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | ID транзакции |
| `user_id` | UUID | ID пользователя |
| `yookassa_payment_id` | TEXT | ID платежа в ЮKassa |
| `status` | TEXT | Статус (pending/succeeded/canceled/failed) |
| `amount` | NUMERIC | Сумма |
| `currency` | TEXT | Валюта (RUB) |
| `payment_type` | TEXT | Тип (initial/recurring/upgrade/one_time) |
| `payment_method_id` | TEXT | ID способа оплаты |
| `error_message` | TEXT | Сообщение об ошибке |
| `metadata` | JSONB | Дополнительные данные |

### Продукты подписок

12 продуктов: 3 тарифа × 4 периода

| Тариф | 1 мес | 3 мес | 6 мес | 12 мес |
|-------|-------|-------|-------|--------|
| Basic | 3999₽ | 11397₽ (-5%) | 21594₽ (-10%) | 40791₽ (-15%) |
| Pro   | 4499₽ | 12822₽ (-5%) | 24294₽ (-10%) | 45891₽ (-15%) |
| Elite | 9999₽ | 28497₽ (-5%) | 53994₽ (-10%) | 101991₽ (-15%) |

---

## 🔧 Настройка

### 1. Переменные окружения

Создайте `.env.local`:

```bash
# Supabase (уже есть)
NEXT_PUBLIC_SUPABASE_URL=...
NEXT_PUBLIC_SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...

# ЮKassa (mock значения для dev)
YOOKASSA_SHOP_ID=mock_shop_id
YOOKASSA_SECRET_KEY=mock_secret_key
NEXT_PUBLIC_YOOKASSA_RETURN_URL=http://localhost:3000/payment/success
YOOKASSA_WEBHOOK_SECRET=mock_webhook_secret

# Cron Job
CRON_SECRET=dev_secret_key_12345

# Email (Resend)
RESEND_API_KEY=re_your_api_key
EMAIL_FROM=MargoFitness <onboarding@resend.dev>
NEXT_PUBLIC_APP_URL=http://localhost:3000
```

### 2. Миграция БД

Выполните в Supabase SQL Editor:

```sql
-- Скопируйте содержимое файла:
database/migrations/008_ADD_YOOKASSA_INTEGRATION.sql
```

Или только продукты:

```sql
-- Если миграция уже была:
database/migrations/INSERT_PRODUCTS_ONLY.sql
```

### 3. Установка зависимостей

```bash
npm install @radix-ui/react-switch @radix-ui/react-checkbox
```

---

## 🧪 Тестирование

### Быстрый тест через UI:

1. Откройте `/pricing`
2. Выберите "1 месяц" → Basic → "Выбрать план"
3. На странице оплаты нажмите "Оплатить"
4. Проверьте Dashboard - должна быть активная подписка

### Полное руководство:

См. `TESTING_GUIDE.md`

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| `ENV_SETUP.md` | Настройка переменных окружения |
| `CRON_SETUP.md` | Настройка cron job для автопродления |
| `WEBHOOK_SETUP.md` | Настройка webhook от ЮKassa |
| `TESTING_GUIDE.md` | Полное руководство по тестированию |
| `tests/test-payment-api.md` | Тестирование API через консоль |
| `EMAIL_SETUP.md` | Настройка email уведомлений (Resend) |
| `EMAIL_TESTING.md` | Тестирование email отправки |

---

## 🚀 Deploy в Production

### Checklist:

- [ ] Зарегистрироваться как ИП
- [ ] Подключить ЮKassa ([yookassa.ru](https://yookassa.ru/))
- [ ] Получить реальные `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`
- [ ] Добавить их в Vercel Environment Variables
- [ ] Настроить Webhook URL в ЮKassa: `https://your-domain.com/api/payments/webhook`
- [ ] Получить `YOOKASSA_WEBHOOK_SECRET` и добавить в Vercel
- [ ] Сгенерировать `CRON_SECRET` и добавить в Vercel
- [ ] Задеплоить на Vercel
- [ ] Проверить что Cron Job запустился (Vercel Dashboard → Cron)
- [ ] Провести тестовый платеж
- [ ] Проверить что webhook сработал
- [ ] Проверить что подписка активировалась

### Важно в production:

1. **Включить проверку подписи webhook:**
   ```typescript
   // В app/api/payments/webhook/route.ts раскомментировать:
   const signature = request.headers.get('X-Yookassa-Signature')
   if (!verifyWebhookSignature(...)) {
     return NextResponse.json({ error: 'Invalid signature' }, { status: 403 })
   }
   ```

2. **Убрать mock режим:**
   - Удалить `mock_` значения из env
   - Использовать реальные ключи ЮKassa

3. **Настроить мониторинг:**
   - Vercel Analytics
   - Error tracking (Sentry)
   - Уведомления о неудачных cron jobs

---

## ✅ Недавно добавлено (Декабрь 2024)

### Email уведомления
- ✅ Интеграция с Resend для отправки email
- ✅ Приветственное письмо после регистрации
- ✅ Уведомление об успешной оплате
- ✅ Уведомление об апгрейде подписки
- ✅ Красивые HTML шаблоны писем

### Апгрейд подписки
- ✅ Умная система конвертации дней
- ✅ UI диалог для апгрейда в Dashboard
- ✅ Ограничения при покупке (нельзя купить тот же или более низкий уровень)
- ✅ Email уведомление об апгрейде с деталями

### Аналитика
- ✅ Страница аналитики в админ-панели
- ✅ Общая выручка и средний чек
- ✅ Выручка по тарифам с визуализацией
- ✅ Статистика подписчиков по уровням
- ✅ Конверсия и новые пользователи
- ✅ Таблица последних транзакций

**Документация:** См. `docs/IMPLEMENTATION_REPORT.md`

---

## 🔄 Что дальше?

### TODO для будущих улучшений:

1. **Email расширения:**
   - Email при неудачном автопродлении
   - Push-уведомления
   - Настройка частоты уведомлений

2. **История платежей:**
   - Детальная страница транзакций для пользователя
   - Скачивание чеков

3. **Аналитика расширения:**
   - Графики выручки по месяцам
   - Cohort analysis
   - LTV метрики
   - Экспорт в CSV

4. **Интеграция:**
   - Apple Pay / Google Pay через ЮКасса
   - СБП (Система быстрых платежей)

---

## 📞 Контакты

Если возникли вопросы по коду, обращайтесь к документации или создайте issue.

**Happy coding! 🚀**

