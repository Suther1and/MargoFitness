# 🏷️ Исправление системы промокодов

## Проблема
При создании промокода возникала ошибка из-за несоответствия типов данных между кодом и базой данных.

## Что было исправлено

### 1. Несоответствие ENUM типов
**Было в базе данных:**
- `'percent'` - процентная скидка
- `'fixed_amount'` - фиксированная сумма

**Было в коде:**
- `'percentage'`
- `'fixed'`

**Исправлено:** Код обновлен для использования правильных значений из базы данных.

### 2. Несоответствие названий колонок
**Было в базе данных:**
- `applicable_products` - JSONB массив ID продуктов

**Было в коде:**
- `applicable_to`

**Исправлено:** Код обновлен для использования правильного названия колонки.

## Измененные файлы

### TypeScript типы
- ✅ `types/supabase.ts` - обновлены ENUM типы и названия колонок

### Server Actions
- ✅ `lib/actions/promo-codes.ts` - обновлена логика создания и валидации промокодов

### UI компоненты
- ✅ `app/admin/promo-codes/create-promo-dialog.tsx` - форма создания промокода
- ✅ `app/admin/promo-codes/promo-codes-list.tsx` - список промокодов
- ✅ `app/payment/[productId]/promo-input.tsx` - применение промокода при оплате

## Применение миграции

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в раздел **SQL Editor**

### Шаг 2: Выполните миграцию
1. Создайте новый запрос
2. Скопируйте содержимое файла `database/migrations/015_FIX_PROMO_CODE_ENUM.sql`
3. Вставьте в редактор
4. Нажмите **Run**

Миграция автоматически:
- ✅ Проверит текущее состояние базы данных
- ✅ Переименует ENUM значения (если нужно)
- ✅ Переименует колонку `applicable_to` → `applicable_products` (если нужно)
- ✅ Добавит колонку `created_by` (если отсутствует)

### Шаг 3: Деплой изменений
```bash
git add .
git commit -m "fix: исправлена система промокодов"
git push
```

Изменения автоматически задеплоятся через GitHub на Vercel.

## Проверка

После применения миграции и деплоя:

1. Откройте админ панель: `/admin/promo-codes`
2. Нажмите **Создать промокод**
3. Заполните форму:
   - Код: `NEWYEAR2025`
   - Тип: Процент
   - Значение: 10%
   - Лимит: 100 (опционально)
4. Нажмите **Создать промокод**

✅ Промокод должен успешно создаться без ошибок!

## Технические детали

### ENUM значения
```typescript
type PromoDiscountType = 'percent' | 'fixed_amount'
```

### Структура таблицы promo_codes
```sql
CREATE TABLE promo_codes (
  id UUID PRIMARY KEY,
  code TEXT NOT NULL UNIQUE,
  discount_type promo_discount_type NOT NULL,
  discount_value NUMERIC(10,2) NOT NULL,
  applicable_products JSONB,  -- массив ID или null
  usage_limit INTEGER,
  usage_count INTEGER DEFAULT 0,
  expires_at TIMESTAMP WITH TIME ZONE,
  is_active BOOLEAN DEFAULT true,
  created_by UUID REFERENCES profiles(id),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

## Дополнительная информация

- **Миграция:** `database/migrations/015_FIX_PROMO_CODE_ENUM.sql`
- **Дата:** 25 декабря 2025
- **Статус:** ✅ Готово к применению

---

**Важно:** Миграция безопасна и не удаляет данные. Существующие промокоды будут автоматически конвертированы в новый формат.

