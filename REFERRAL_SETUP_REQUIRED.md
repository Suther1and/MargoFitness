# ⚠️ КРИТИЧЕСКАЯ НАСТРОЙКА: SERVICE_ROLE_KEY

## Проблема
Реферальная система **НЕ РАБОТАЕТ** без `SUPABASE_SERVICE_ROLE_KEY`!

## Почему это важно?
- Триггер БД создает бонусный аккаунт ✅
- НО реферальные связи требуют backend операций ❌
- API endpoints не могут обойти RLS без service role key

## Как исправить (5 минут):

### 1. Откройте Supabase Dashboard
https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

### 2. Найдите секцию "Project API keys"
Там будет 2 ключа:
- `anon` / `public` - уже используется ✅
- **`service_role`** - нужен для реферальной системы ❌

### 3. Скопируйте `service_role` key
⚠️ Это секретный ключ! Никогда не коммитьте в Git!

### 4. Добавьте в `.env.local`:
```bash
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InlvdXItcHJvamVjdCIsInJvbGUiOiJzZXJ2aWNlX3JvbGUiLCJpYXQiOjE2...
```

### 5. Добавьте в Vercel Environment Variables:
1. Vercel Dashboard → Settings → Environment Variables
2. Добавьте: `SUPABASE_SERVICE_ROLE_KEY` = `ваш_ключ`
3. Redeploy приложение

### 6. Перезапустите dev сервер
```bash
npm run dev
```

## Как проверить что работает:

### 1. Проверьте логи при старте:
```
✅ Должно быть: Нет ошибок про SERVICE_ROLE_KEY
❌ Было: [Process Referral] SUPABASE_SERVICE_ROLE_KEY not found!
```

### 2. Протестируйте регистрацию:
1. Скопируйте реферальную ссылку из `/dashboard/bonuses`
2. Откройте в инкогнито
3. Зарегистрируйтесь
4. Проверьте логи:
   - `[Process Referral] Request: { userId: '...', refCode: '...' }`
   - `[Process Referral] Success for user: ...`
5. Проверьте баланс: должно быть **500 шагов**
6. Проверьте таблицу `referrals` в Supabase - должна появиться запись

## Что делает service_role key:

### Без него (текущее состояние):
- ✅ Профиль создается
- ✅ Бонусный аккаунт создается (250 шагов)
- ✅ Реферальный код генерируется
- ❌ Реферальная связь НЕ создается
- ❌ Бонус 250 шагов за приглашение НЕ начисляется
- ❌ Таблица `referrals` остается пустой

### С ним (как должно быть):
- ✅ Все вышеперечисленное
- ✅ Создается запись в `referrals`
- ✅ Начисляется 250 шагов бонусом
- ✅ Итого: 500 шагов (250 welcome + 250 referral)
- ✅ Приглашающий получит бонусы при покупке реферала

## Альтернативное решение (если нет доступа к service_role):

Можно создать Edge Function в Supabase, но это сложнее:
1. Supabase Dashboard → Edge Functions
2. Создать функцию `process-referral`
3. Переписать логику на Deno
4. Изменить API endpoints

**Рекомендую просто добавить SERVICE_ROLE_KEY - это 2 минуты!**

## Безопасность:

⚠️ **ВАЖНО:**
- Service role key обходит все RLS политики
- Используется ТОЛЬКО на backend (API routes)
- НИКОГДА не отправляется на клиент
- НИКОГДА не коммитится в Git
- Хранится только в `.env.local` и Vercel

## После настройки:

Система будет работать так:
1. Пользователь регистрируется по реферальной ссылке
2. Триггер БД создает профиль + бонусный аккаунт (250 шагов)
3. API с service_role key:
   - Проверяет реферальный код
   - Создает связь в `referrals`
   - Начисляет 250 шагов бонусом
4. Итого: 500 шагов на балансе
5. При покупке реферала - приглашающий получит бонусы

---

**Статус:** ⚠️ ТРЕБУЕТСЯ НАСТРОЙКА
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
**Время:** ⏱️ 5 минут

