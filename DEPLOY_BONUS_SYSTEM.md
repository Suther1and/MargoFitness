# 🚀 Деплой бонусной и реферальной систем

## Что было добавлено

✅ Бонусная валюта "Шаги" 👟 (1 шаг = 1₽)
✅ Многоуровневый кешбек (3-10%)
✅ Реферальная программа с уникальными ссылками
✅ Система промокодов
✅ Страница `/dashboard/bonuses`
✅ Интеграция в оплату с калькулятором
✅ Админка промокодов `/admin/promo-codes`
✅ Аналитика бонусной системы

---

## 📋 Чек-лист для деплоя

### ✅ Шаг 1: Подготовка проекта (выполнено автоматически)

- [x] Установлены все зависимости
- [x] Созданы все необходимые файлы
- [x] Миграция БД готова
- [x] Код закоммичен в Git

### 🔧 Шаг 2: Применение миграции в Supabase (ВЫПОЛНИТЕ ВРУЧНУЮ)

1. **Откройте Supabase Dashboard:**
   - Перейдите на https://supabase.com
   - Выберите ваш проект MargoFitness

2. **Откройте SQL Editor:**
   - В левом меню выберите "SQL Editor"
   - Нажмите "New query"

3. **Скопируйте миграцию:**
   - Откройте файл `database/migrations/013_ADD_BONUS_REFERRAL_SYSTEM.sql`
   - Скопируйте ВСЁ содержимое (все 416 строк)

4. **Выполните миграцию:**
   - Вставьте в SQL Editor
   - Нажмите "Run" (или Ctrl+Enter)
   - Дождитесь выполнения (займет 5-10 секунд)

5. **Проверьте результат:**
   Вы должны увидеть в логах:
   ```
   ✅ Миграция 013 успешно завершена!
   Создано:
     • 5 новых таблиц с RLS
     • 3 новых ENUM типа
     • Триггеры и функции
     • Бонусных счетов: X (количество существующих пользователей)
     • Реферальных кодов: X
   
   Готово к использованию! 👟
   ```

6. **Проверьте созданные таблицы:**
   - В левом меню "Table Editor"
   - Должны появиться новые таблицы:
     - `user_bonuses`
     - `bonus_transactions`
     - `referral_codes`
     - `referrals`
     - `promo_codes`

### 🌐 Шаг 3: Проверка переменных окружения в Vercel (ПРОВЕРЬТЕ)

Убедитесь, что в настройках проекта Vercel (Settings → Environment Variables) есть:

**Обязательные переменные (уже должны быть):**
- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `NEXT_PUBLIC_SITE_URL`
- `YOOKASSA_SHOP_ID`
- `YOOKASSA_SECRET_KEY`

**Новые переменные НЕ требуются** - бонусная система использует существующие.

### 🚀 Шаг 4: Деплой на Vercel

**Через Git (автоматический деплой):**

Код уже закоммичен и запушен. После пуша в `main` ветку:
1. Vercel автоматически начнет деплой
2. Перейдите на https://vercel.com/dashboard
3. Откройте ваш проект
4. Дождитесь завершения деплоя (обычно 2-3 минуты)

**ИЛИ через Vercel CLI (если нужно):**
```bash
vercel --prod
```

---

## 🧪 Тестирование после деплоя

### Тест 1: Регистрация и приветственный бонус

1. Зарегистрируйте нового пользователя
2. Перейдите в `/dashboard/bonuses`
3. **Ожидание:** Баланс = 250 шагов, уровень Bronze (3%)

### Тест 2: Реферальная ссылка

1. На странице `/dashboard/bonuses` скопируйте реферальную ссылку
2. В приватном окне/другом браузере откройте эту ссылку
3. **Ожидание:** Toast уведомление "🎁 [Имя] пригласил вас! Получите 250 шагов"
4. Зарегистрируйтесь
5. Проверьте баланс нового пользователя: должно быть 250 шагов

### Тест 3: Покупка и кешбек

1. Купите подписку Basic 1 месяц (3999₽)
2. После успешной оплаты проверьте бонусы
3. **Ожидание:** Кешбек 3% = 119 шагов (итого 369 шагов)
4. В истории должна быть запись о кешбеке

### Тест 4: Реферальное вознаграждение

1. Реферал (пользователь из теста 2) покупает подписку
2. Проверьте бонусы приглашающего
3. **Ожидание:** 
   - +500 шагов (разовый бонус за первого реферала)
   - +119 шагов (3% от покупки)

### Тест 5: Промокоды (админ)

1. Войдите как админ
2. Перейдите в `/admin/promo-codes`
3. Создайте промокод: TESTDEPLOY, скидка 10%
4. Выйдите из админки
5. При оплате введите промокод
6. **Ожидание:** Скидка применится

### Тест 6: Использование шагов

1. Накопите минимум 500 шагов
2. Перейдите на страницу оплаты любой подписки
3. В разделе "Использовать шаги" введите 500
4. **Ожидание:** Итоговая сумма уменьшится на 500₽

### Тест 7: Аналитика (админ)

1. Перейдите в `/admin/analytics`
2. Пролистайте вниз до секции "Бонусная программа"
3. **Ожидание:** Видны метрики: выдано шагов, потрачено, рефералы

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Миграция не применилась

**Симптомы:** 
- Ошибка при запуске миграции
- Таблицы не создались

**Решение:**
1. Проверьте, что скопировали ВСЮ миграцию (416 строк)
2. Убедитесь, что у вас права admin в Supabase
3. Попробуйте выполнить миграцию частями (по разделам)

### Проблема 2: Бонусы не начисляются

**Симптомы:**
- После покупки нет кешбека
- У новых пользователей нет приветственного бонуса

**Решение:**
1. Проверьте логи webhook: `/api/payments/webhook`
2. Проверьте, что триггер `on_profile_created_create_bonus` создан:
   ```sql
   SELECT * FROM pg_trigger WHERE tgname = 'on_profile_created_create_bonus';
   ```
3. Проверьте RLS политики на таблицах

### Проблема 3: Реферальная ссылка не работает

**Симптомы:**
- При переходе по ссылке нет уведомления
- Бонус не начисляется

**Решение:**
1. Проверьте, что `NEXT_PUBLIC_SITE_URL` правильно настроен в Vercel
2. Проверьте таблицу `referral_codes` - есть ли код?
3. Проверьте `/auth/callback` - передается ли параметр `ref`

### Проблема 4: Промокоды не применяются

**Симптомы:**
- Ошибка "Промокод не найден"

**Решение:**
1. Проверьте, что промокод активен (`is_active = true`)
2. Проверьте срок действия (`expires_at`)
3. Проверьте лимит использований

---

## 📊 Мониторинг после деплоя

### Проверьте через день после деплоя:

1. **Количество пользователей с бонусами:**
   ```sql
   SELECT COUNT(*) FROM user_bonuses;
   ```
   Должно равняться количеству пользователей

2. **Общий баланс в системе:**
   ```sql
   SELECT SUM(balance) as total_balance FROM user_bonuses;
   ```

3. **Количество транзакций:**
   ```sql
   SELECT type, COUNT(*) 
   FROM bonus_transactions 
   GROUP BY type;
   ```

4. **Активность реферальной программы:**
   ```sql
   SELECT 
     COUNT(*) as total_referrals,
     COUNT(*) FILTER (WHERE status = 'first_purchase_made') as active
   FROM referrals;
   ```

---

## 📝 Дополнительная документация

- **Полное руководство:** `BONUS_SYSTEM_README.md`
- **Примеры использования API:** см. файлы в `lib/actions/`
- **Структура БД:** см. `database/migrations/013_ADD_BONUS_REFERRAL_SYSTEM.sql`

---

## 🎉 Готово!

После выполнения всех шагов бонусная система будет полностью функциональна в продакшене.

**Следующие шаги для улучшения (опционально):**
- Добавить email уведомления о начислении бонусов
- Добавить реал-тайм обновление баланса
- Реализовать бонусы за выполнение тренировок
- Добавить бонусы за серии (streak)

**Поддержка:**
Если возникнут вопросы или проблемы - проверьте раздел "Возможные проблемы" выше.

