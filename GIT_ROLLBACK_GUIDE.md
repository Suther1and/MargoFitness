# 🔄 Гайд по откату изменений

## Текущая структура бранчей

```
master (v1.0-stable) ← Стабильная версия (работает всё)
  └── feature/optimization ← Текущий бранч (для экспериментов)
```

---

## 🚨 Если что-то сломалось

### Вариант 1: Откатиться к стабильной версии

```bash
# Вернуться на стабильный бранч
git checkout master

# Или напрямую к тегу
git checkout v1.0-stable
```

После этого команда `npm run dev` запустит проект в стабильном состоянии.

---

### Вариант 2: Отменить последние изменения (но остаться на feature/optimization)

```bash
# Посмотреть последние коммиты
git log --oneline -5

# Откатить последний коммит (мягко, сохранив файлы)
git reset --soft HEAD~1

# Откатить последний коммит (жестко, удалив изменения)
git reset --hard HEAD~1

# Откатить к конкретному коммиту
git reset --hard <commit-hash>
```

---

### Вариант 3: Создать новую копию от стабильной версии

```bash
# Вернуться на master
git checkout master

# Создать новый бранч
git checkout -b feature/optimization-v2

# Продолжить работу в новом бранче
```

---

## ✅ Проверка текущего состояния

```bash
# На каком бранче мы сейчас?
git branch

# Что изменилось?
git status

# История коммитов
git log --oneline --graph --all

# Разница между бранчами
git diff master feature/optimization
```

---

## 💾 Сохранить прогресс перед откатом

Если хотите сохранить текущую работу, но откатиться:

```bash
# Сохранить изменения во временное хранилище
git stash save "WIP: optimization attempt"

# Откатиться на master
git checkout master

# Потом вернуться и восстановить
git checkout feature/optimization
git stash pop
```

---

## 🔖 Создать промежуточный checkpoint

Если хотите создать еще один checkpoint в процессе:

```bash
# Сохранить текущее состояние
git add .
git commit -m "Checkpoint: после добавления админ-панели пользователей"

# Создать тег
git tag -a v1.1-users-panel -m "Added users management panel"
```

---

## 🌐 Supabase Rollback

Для отката базы данных:

1. Откройте **Supabase Dashboard**
2. **Database** → **Backups**
3. Найдите бэкап `before-optimization`
4. Нажмите **Restore**

---

## 📋 Быстрая справка

| Команда | Что делает |
|---------|------------|
| `git checkout master` | Вернуться к стабильной версии |
| `git checkout v1.0-stable` | То же самое (через тег) |
| `git reset --hard HEAD~1` | Удалить последний коммит |
| `git diff master` | Посмотреть, что изменилось от master |
| `git stash` | Временно отложить изменения |
| `git log --oneline` | История коммитов |

---

## ⚠️ ВАЖНО

- Бранч **`master`** - всегда стабильный (не трогайте его!)
- Бранч **`feature/optimization`** - для экспериментов
- Тег **`v1.0-stable`** - точка возврата

**Перед любым опасным действием делайте:**
```bash
git add .
git commit -m "Checkpoint: перед опасным действием"
```

---

## 🎯 Готово!

Теперь можно спокойно экспериментировать. В любой момент один из этих способов вернет проект в рабочее состояние! 🚀

