# ✅ Реферальная система - Финальная версия

## Статус: РАБОТАЕТ 🎉

Система полностью функциональна и протестирована.

## Архитектура

### 1. Регистрация по реферальной ссылке

**Флоу:**
```
1. Пользователь открывает: /auth?ref=abc12345
2. Ref код валидируется и сохраняется в localStorage
3. После регистрации → редирект в /dashboard
4. ReferralProcessor (невидимый компонент):
   - Читает ref из localStorage
   - Ждет 2 сек (установка сессии)
   - Вызывает /api/auth/process-referral-client
   - Создает запись в referrals
   - Начисляет 250 шагов
   - Показывает уведомление
   - Перезагружает страницу
5. Итоговый баланс: 500 шагов (250 welcome + 250 referral)
```

**Файлы:**
- `lib/actions/referrals.ts` - `getReferralLink()` - генерация ссылки
- `app/auth/page.tsx` - сохранение ref в localStorage
- `components/referral-processor.tsx` - обработка после входа
- `app/api/auth/process-referral-client/route.ts` - API endpoint

### 2. Покупка реферала

**Флоу:**
```
1. Реферал совершает покупку
2. ЮKassa webhook → /api/payments/webhook
3. handleReferralPurchase() с admin client:
   - Находит связь в referrals
   - Рассчитывает процент (3-10%)
   - Начисляет бонус приглашающему
   - Если первая покупка → +500 шагов разово
   - Обновляет статус реферала
   - Обновляет total_referral_earnings
```

**Файлы:**
- `app/api/payments/webhook/route.ts` - обработка webhook
- `lib/actions/referrals.ts` - `handleReferralPurchase()`
- `lib/actions/bonuses.ts` - `awardCashback()`

## Компоненты системы

### База данных (Supabase)

**Таблицы:**
```sql
referral_codes
├─ user_id (UUID, unique)
├─ code (TEXT, unique) -- 8 символов
└─ created_at

referrals
├─ referrer_id (UUID) -- кто пригласил
├─ referred_id (UUID, unique) -- кого пригласил
├─ status (ENUM) -- 'registered' | 'first_purchase_made'
├─ created_at
└─ first_purchase_at

user_bonuses
├─ user_id (UUID)
├─ balance (INTEGER) -- текущий баланс шагов
├─ referral_level (INTEGER) -- 1-4
└─ total_referral_earnings (NUMERIC) -- для расчета уровня

bonus_transactions
├─ user_id (UUID)
├─ amount (INTEGER) -- +/- шаги
├─ type (ENUM) -- 'welcome', 'referral_bonus', 'referral_first', etc
├─ description (TEXT)
├─ related_user_id (UUID) -- для реферальных
└─ created_at
```

**Триггер:**
```sql
on_profile_created_create_bonus
-- Автоматически создает:
-- 1. user_bonuses (balance: 250)
-- 2. referral_codes (code: random)
-- 3. bonus_transactions (welcome: 250)
```

### Backend

**API Endpoints:**
- `POST /api/auth/process-referral-client` - обработка реферала после регистрации
- `POST /api/payments/webhook` - обработка покупок (кешбек + реферальные бонусы)

**Server Actions:**
```typescript
// lib/actions/referrals.ts
getReferralLink(userId) // Генерация ссылки
validateReferralCode(code) // Проверка кода
registerReferral(code, userId) // Создание связи (не используется)
handleReferralPurchase(userId, amount, client?) // Обработка покупки
getReferralStats(userId) // Статистика для UI

// lib/actions/bonuses.ts
awardCashback(params, client?) // Начисление кешбека
addBonusTransaction(params) // Создание транзакции
getUserBonusAccount(userId) // Получение счета
```

### Frontend

**Компоненты:**
- `app/auth/page.tsx` - страница авторизации с обработкой ref
- `components/referral-processor.tsx` - невидимый процессор реферала
- `app/dashboard/bonuses/referral-section.tsx` - UI реферальной программы
- `app/dashboard/bonuses/page.tsx` - страница бонусов

## Бонусы

### Для приглашенного (referred):
- **250 шагов** - приветственный бонус (автоматически)
- **250 шагов** - за регистрацию по реферальной ссылке
- **Итого: 500 шагов** при регистрации

### Для приглашающего (referrer):
- **3-10%** от каждой покупки реферала (зависит от уровня)
- **500 шагов** - разовый бонус за первую покупку первого реферала
- Повышение уровня реферальной программы

### Уровни реферальной программы:
```
Уровень 1: 3% (0-10,000₽ покупок рефералов)
Уровень 2: 5% (10,000-50,000₽)
Уровень 3: 7% (50,000-100,000₽)
Уровень 4: 10% (100,000₽+)
```

## Безопасность

### RLS (Row Level Security):
- ✅ Все таблицы защищены RLS
- ✅ Пользователи видят только свои данные
- ✅ Админы имеют полный доступ

### Service Role Key:
- ✅ Используется только на backend
- ✅ Передается в функции через параметр
- ✅ Webhook использует service role для обхода RLS

### Валидация:
- ✅ Нельзя использовать свой реферальный код
- ✅ Нельзя зарегистрироваться дважды по реферальной ссылке
- ✅ Проверка существования кода перед созданием связи

## Оптимизации

### Что было убрано:
- ❌ Избыточное логирование в production
- ❌ Debug endpoint `/api/debug/referral-check`
- ❌ Старый endpoint `/api/auth/process-referral`
- ❌ Дублирующиеся console.log в клиентском коде

### Что оставлено:
- ✅ Критичное логирование ошибок
- ✅ Логи в webhook для отладки платежей
- ✅ Логи в handleReferralPurchase для отслеживания бонусов

## Переменные окружения

**Обязательные:**
```bash
NEXT_PUBLIC_SUPABASE_URL=https://xxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc... # КРИТИЧНО!
NEXT_PUBLIC_SITE_URL=https://margofitness.pro
```

**Для OAuth (опционально):**
```bash
YANDEX_CLIENT_ID=xxx
YANDEX_CLIENT_SECRET=xxx
TELEGRAM_BOT_TOKEN=xxx
NEXT_PUBLIC_TELEGRAM_BOT_NAME=xxx
```

## Тестирование

### 1. Регистрация по реферальной ссылке:
```bash
1. Войти как пользователь A
2. /dashboard/bonuses → скопировать ссылку
3. Открыть в инкогнито
4. Зарегистрироваться
5. Проверить: баланс = 500 шагов
6. Проверить: в таблице referrals есть запись
7. Проверить: у пользователя A "Приглашено друзей" = 1
```

### 2. Покупка реферала:
```bash
1. Зарегистрированный реферал совершает покупку
2. Проверить Vercel logs webhook
3. Проверить: приглашающий получил бонусы
4. Первая покупка: 3% + 500 шагов
5. Последующие: только 3%
6. Статус "Совершили покупку" обновился
```

## Известные ограничения

1. **localStorage** - не работает если браузер блокирует
2. **2 секунды таймаут** - может быть мало на медленном интернете
3. **Один реферал** - пользователь может зарегистрироваться только по одной ссылке
4. **Email уникальность** - один email = один аккаунт

## Поддержка

При проблемах проверить:
1. ✅ `SUPABASE_SERVICE_ROLE_KEY` настроен в Vercel
2. ✅ Реферальная ссылка содержит `?ref=код`
3. ✅ Триггер `on_profile_created_create_bonus` активен
4. ✅ Таблицы `referrals`, `referral_codes`, `user_bonuses` существуют
5. ✅ RLS политики настроены правильно

## Метрики

Для мониторинга системы:
- Количество регистраций по реферальным ссылкам
- Конверсия реферал → первая покупка
- Средний чек реферала
- Топ рефереров (админка)

---

**Версия:** 1.0 (финальная)
**Дата:** 25.12.2024
**Статус:** ✅ Production Ready

