# ⚡ СРОЧНО: Исправление NEXT_PUBLIC_SITE_URL в Vercel

## 🔴 Проблема
Google OAuth перекидывает на localhost, потому что в Vercel установлен **неправильный домен** или его вообще нет.

## ✅ Решение (1 минута)

### В Vercel Dashboard:

1. Откройте ваш проект в Vercel
2. **Settings → Environment Variables**
3. Найдите `NEXT_PUBLIC_SITE_URL` или добавьте новую переменную
4. Установите значение:
   ```
   https://www.margofitness.pro
   ```
   (с `www.` если используете, или без если редирект настроен)

5. **Выберите окружения:** Production, Preview, Development (все три)
6. **Сохраните**
7. **Redeploy:** Deployments → три точки на последнем деплое → Redeploy

---

## 🔍 Что проверить в Supabase

**Authentication → URL Configuration:**
- **Site URL:** `https://www.margofitness.pro`
- **Redirect URLs:** `https://www.margofitness.pro/**`

(Если используете без `www.`, то везде без него)

---

## ⏱️ Время обновления

- **Vercel redeploy:** 2-3 минуты
- **Google OAuth кэш:** может занять 5-10 минут
- **Supabase:** мгновенно после сохранения

---

## 🧪 Проверка после деплоя

Откройте консоль браузера на сайте:
```javascript
console.log(process.env.NEXT_PUBLIC_SITE_URL)
// Должно вывести: "https://www.margofitness.pro"
```

Если выводит `undefined` - переменная не установлена в Vercel!

---

## ✅ Итог

**Сейчас:** `NEXT_PUBLIC_SITE_URL` не установлен или установлен на vercel.app  
**Нужно:** `https://www.margofitness.pro`  
**Где:** Vercel → Settings → Environment Variables → Redeploy

**Telegram:** Если был настроен и работал - должен продолжить работать после редеплоя.


