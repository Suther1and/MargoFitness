# 🔄 Рекуррентные платежи ЮКасса

## 📋 Текущий статус

### ✅ Что реализовано в коде:

1. **Сохранение карты при оплате**
   - `lib/services/yookassa.ts`: функция `createPayment()` с `save_payment_method: true`
   - Карта автоматически сохраняется для всех подписок

2. **Рекуррентные платежи**
   - `lib/services/yookassa.ts`: функция `createRecurrentPayment()`
   - Использует сохраненный `payment_method_id` для автосписания

3. **Автопродление подписок**
   - `lib/services/subscription-manager.ts`: функция `autoRenewSubscription()`
   - Автоматически продлевает подписку за день до окончания
   - Обрабатывает неудачные попытки списания

4. **Cron job для автопродления**
   - `app/api/cron/renew-subscriptions/route.ts`
   - Настроен на запуск каждый день в 00:00 UTC через Vercel Cron
   - `vercel.json`: `"schedule": "0 0 * * *"`

5. **Отображение карты в UI**
   - `app/dashboard/subscription-manager.tsx`: блок "Привязанная карта"
   - Показывается только если `payment_method_id` присутствует в профиле
   - Переключатель "Автоматическое продление"

## ⚠️ Ограничения тестового магазина ЮКассы

### Важно знать:

1. **Рекуррентные платежи требуют дополнительного соглашения с ЮКассой**
   - Не подключаются автоматически
   - Даже в тестовом магазине нужно специальное разрешение
   - Без соглашения ЮКасса не будет обрабатывать рекуррентные операции

2. **Тестовый магазин может не возвращать `payment_method_id`**
   - После успешной оплаты в webhook приходит `payment.payment_method?.id`
   - В тестовом магазине без подключенной функции рекуррентов это поле может быть `null`
   - Это нормально для тестового режима

3. **Для production нужно:**
   - Написать в поддержку ЮКассы
   - Заключить дополнительное соглашение о рекуррентных платежах
   - Активировать функцию в настройках аккаунта
   - После этого `payment_method_id` будет приходить корректно

## 🧪 Как протестировать отображение карты

### Вариант 1: Через development кнопку

1. В режиме разработки (`NODE_ENV=development`)
2. Зайдите в Dashboard
3. Оформите любую подписку
4. После успешной оплаты нажмите кнопку **"🧪 [DEV] Сохранить тестовую карту"**
5. Карта появится в блоке "Привязанная карта"
6. Переключатель "Автоматическое продление" станет доступен

### Вариант 2: Через Supabase Dashboard

1. Откройте Supabase Dashboard → Table Editor → `profiles`
2. Найдите вашего пользователя
3. Добавьте в поле `payment_method_id` любое значение, например: `test_card_123`
4. Обновите страницу Dashboard
5. Карта должна отобразиться

### Вариант 3: Через реальную ЮКассу (после подключения рекуррентов)

1. Заключите соглашение с ЮКассой о рекуррентных платежах
2. Включите функцию в настройках магазина
3. Оформите тестовую подписку
4. ЮКасса вернет реальный `payment_method_id`
5. Карта автоматически отобразится в Dashboard

## 🔍 Как проверить что приходит от ЮКассы

### В логах Vercel:

После успешной оплаты в логах webhook будет:

```
[Webhook] Received event: payment.succeeded, payment: 2c9...
[Webhook] Full payment object: { ... }
[Webhook] Payment method: { id: "...", type: "bank_card", saved: true }
```

Обратите внимание на поле `payment.payment_method`:
- Если есть `id` → карта успешно сохранена ✅
- Если `null` или отсутствует → рекуррентные платежи не подключены ⚠️

### В Supabase Database:

Проверьте таблицу `profiles`:
- Поле `payment_method_id` → должно содержать ID карты
- Поле `auto_renew_enabled` → должно быть `true` если карта сохранена

Проверьте таблицу `payment_transactions`:
- Поле `payment_method_id` → ID карты использованной для оплаты
- Поле `payment_type` → `initial` для первого платежа, `recurring` для автосписания

## 🚀 Как работает автопродление

### Алгоритм:

1. **Каждый день в 00:00 UTC** запускается cron job (`/api/cron/renew-subscriptions`)

2. **Поиск подписок для продления:**
   - Ищет профили где `subscription_status = 'active'`
   - И `auto_renew_enabled = true`
   - И `next_billing_date` завтра
   - И `payment_method_id` не пустой

3. **Попытка списания:**
   - Вызывает `createRecurrentPayment()` с сохраненным `payment_method_id`
   - ЮКасса пытается списать деньги с привязанной карты
   - Без подтверждения от пользователя (3D-Secure не требуется)

4. **Обработка результата:**
   - **Успех:** Обновляет подписку, продлевает на новый период
   - **Неудача:** Увеличивает счетчик `failed_payment_attempts`
   - После 3 неудач: Переводит на Free тариф

### Код для ручного тестирования автопродления:

```typescript
// В консоли разработчика или через API endpoint
const response = await fetch('/api/cron/renew-subscriptions', {
  method: 'GET',
  headers: {
    'Authorization': `Bearer ${process.env.CRON_SECRET}` // Из .env.local
  }
})
const result = await response.json()
console.log(result)
```

## 📝 Что добавить в production

### 1. Email уведомления:

- ✅ Успешное продление (уже реализовано)
- ⚠️ Неудачная попытка списания (TODO)
- ⚠️ Последняя попытка перед отключением (TODO)
- ⚠️ Подписка отключена из-за неоплаты (TODO)

### 2. Управление картами:

- ⚠️ Возможность удалить привязанную карту (TODO)
- ⚠️ Отображение последних 4 цифр карты (требует доп. API ЮКассы)
- ⚠️ Срок действия карты (TODO)

### 3. История автосписаний:

- ✅ История транзакций (уже есть)
- ⚠️ Фильтр по типу платежа (initial/recurring) (TODO)
- ⚠️ Детали неудачных попыток (TODO)

## 🔗 Полезные ссылки

- [Документация ЮКасса - Рекуррентные платежи](https://yookassa.ru/docs/support/payments/recurring-payments)
- [Документация ЮКасса - Сохранение платежных данных](https://yookassa.ru/docs/support/payments/save-payment-method)
- [API ЮКасса - Создание платежа](https://yookassa.ru/developers/api#create_payment)
- [Тестовый магазин ЮКасса](https://yookassa.ru/docs/support/merchant/payments/implement/test-store)

## ⚙️ Переменные окружения

```env
# ЮКасса
YOOKASSA_SHOP_ID=1236232
YOOKASSA_SECRET_KEY=test_TVB8TaaAmH5opZrt-O4xicPqgoIYnUSCTIWtIG5kR4E
YOOKASSA_WEBHOOK_SECRET=  # Опционально для тестового магазина
NEXT_PUBLIC_YOOKASSA_RETURN_URL=https://your-domain.com/dashboard?payment=success

# Cron Secret (для защиты endpoint автопродления)
CRON_SECRET=your_secret_here

# Supabase Service Role (для обхода RLS в webhook и cron)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
```

## 📊 Схема работы

```
Пользователь оформляет подписку
           ↓
ЮКасса сохраняет карту (если подключены рекуррентные платежи)
           ↓
payment_method_id возвращается в webhook
           ↓
Сохраняется в profiles.payment_method_id
           ↓
auto_renew_enabled = true
           ↓
Cron job каждый день проверяет next_billing_date
           ↓
За день до окончания пытается списать с карты
           ↓
        Успех?
       /      \
     Да        Нет
      ↓         ↓
 Продлено   Счетчик++
              ↓
         3 попытки?
              ↓
         Free тариф
```

## ✅ Чеклист для production

- [ ] Заключить соглашение с ЮКассой о рекуррентных платежах
- [ ] Активировать функцию в настройках магазина
- [ ] Протестировать сохранение карты
- [ ] Протестировать автосписание
- [ ] Настроить email уведомления о неудачных попытках
- [ ] Добавить управление картами в UI
- [ ] Настроить мониторинг cron job
- [ ] Протестировать полный цикл продления
- [ ] Обновить privacy policy и terms of service

