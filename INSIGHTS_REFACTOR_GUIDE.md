# 🎯 Гайд по доработке старых карточек персональных инсайтов

## 📋 Контекст

### ✅ Что уже сделано:
Реализованы **новые карточки** с расширенной системой инсайтов (по новым стандартам):
- **Сон** (`stats-sleep.tsx`) - 5 вариантов главного блока + анализ паттернов
- **Кофеин** (`stats-caffeine.tsx`) - 5 вариантов + анализ паттернов
- **Настроение** (`stats-mood.tsx`) - 5 вариантов + анализ динамики
- **Питание** (`stats-nutrition.tsx`) - 9 вариантов (3 типа целей × 3 уровня успеха) + анализ стабильности

### 🔨 Что нужно доработать:
**Старые карточки** с базовой системой (по 3 варианта, устаревшие):
1. **Привычки** (`stats-habits.tsx`)
2. **Шаги** (`stats-steps.tsx`)
3. **Вода** (`stats-water.tsx`)
4. **Вес** (`stats-weight.tsx`)

---

## 🏗️ Новая структура (стандарт)

### **Layout:**
```tsx
<div className="grid gap-6 lg:grid-cols-2">
  {/* Левая колонка: График */}
  <div>...</div>
  
  {/* Правая колонка: Инсайты */}
  <div className="space-y-6">
    {/* Блок 1: Главная оценка */}
    {/* Блок 2: Анализ паттернов (2 карточки) */}
    {/* Блок 3: Практические рекомендации */}
    {/* Блок 4: Образовательный блок */}
  </div>
</div>
```

### **Структура блоков:**

#### **Блок 1: Главная оценка** (цветной фон)
- **5+ вариантов** в зависимости от ключевой метрики
- Цвета: `emerald` (отлично), `blue` (хорошо), `amber` (средне), `orange` (ниже среднего), `red` (плохо)
- Иконка + заголовок + описание
- Динамический цвет фона

#### **Блок 2: Анализ паттернов** (2 карточки без фона)
- Два аспекта метрики (стабильность, точность, экстремумы, тренды)
- По **3 варианта** для каждой карточки
- Динамический цвет заголовка/иконки (без фона)

#### **Блок 3: Практические рекомендации**
- **5 контекстных советов** в зависимости от главной метрики
- Адаптивные под ситуацию пользователя

#### **Блок 4: Образовательный**
- **3 варианта** общей информации/влияния метрики на здоровье
- Могут быть привязаны к цели/типу активности

---

## 🔄 Процесс работы (как делали)

### **1. Анализ (в чате)**
```
1. Прочитать текущий файл stats-[metric].tsx
2. Описать существующие блоки и их варианты
3. Определить зависимости (от каких метрик зависит вариант)
4. Выявить проблемы (устаревшие инсайты, недостающие)
```

### **2. Проектирование (согласование с пользователем)**
```
Предложить:
- Какие блоки оставить/изменить/удалить
- Новые варианты для каждого блока (расписать текст)
- Новые метрики если нужны
- Условия переключения вариантов
```

### **3. Реализация (после согласования)**
```
1. Изменить layout на двухколоночный
2. Реализовать 5+ вариантов Блока 1
3. Реализовать 2 карточки Блока 2 (по 3 варианта каждая)
4. Реализовать 5 вариантов Блока 3
5. Реализовать 3 варианта Блока 4
6. Применить динамическое окрашивание
7. Закоммитить: `feat: refactor [metric] personal insights with extended variants`
```

---

## 🎨 Технические паттерны

### **Динамическое окрашивание:**

**Блок 1 (с фоном):**
```tsx
{condition1 ? (
  <Card className="bg-gradient-to-br from-emerald-500/10 to-emerald-500/5 border-emerald-500/20">
    <div className="flex items-start gap-3">
      <div className="p-2 rounded-lg bg-emerald-500/20">
        <Icon className="w-5 h-5 text-emerald-400" />
      </div>
      <div>
        <h3 className="font-bold text-white">Заголовок</h3>
        <p className="text-white/60 text-sm">Описание</p>
      </div>
    </div>
  </Card>
) : condition2 ? (
  // ... blue вариант
) : condition3 ? (
  // ... amber вариант
) : (
  // ... red вариант
)}
```

**Блок 2 (без фона, цветные заголовки):**
```tsx
<Card>
  <div className="flex items-center gap-2 mb-3">
    <Icon className={cn(
      "w-5 h-5",
      condition1 ? "text-emerald-400" : 
      condition2 ? "text-amber-400" : "text-red-400"
    )} />
    <h3 className={cn(
      "font-bold",
      condition1 ? "text-emerald-400" : 
      condition2 ? "text-amber-400" : "text-red-400"
    )}>Заголовок</h3>
  </div>
  <p className="text-white/60 text-sm">
    {condition1 ? "Текст 1" : condition2 ? "Текст 2" : "Текст 3"}
  </p>
</Card>
```

### **Метрики для расчётов:**
```tsx
const avgDaily = Math.round(total / data.length)
const goal = settings.widgets.[metric]?.goal || defaultGoal
const achievementRate = Math.round((daysAchieved / data.length) * 100)
const bestDay = data.reduce((max, day) => day.value > max.value ? day : max)
const worstDay = data.reduce((min, day) => day.value < min.value ? day : min)
const range = maxValue - minValue
const consistency = calculateStdDev(data) // для стабильности
```

---

## 📝 Чеклист для каждой карточки

### **Перед началом:**
- [ ] Прочитать `stats-[metric].tsx`
- [ ] Описать текущие блоки (сколько, какие условия, какие тексты)
- [ ] Проверить актуальность (есть ли устаревшие инсайты)
- [ ] Определить недостающие важные инсайты

### **Проектирование:**
- [ ] Предложить структуру 4 блоков
- [ ] Расписать 5+ вариантов для Блока 1 (текст + условия)
- [ ] Расписать 2 карточки Блока 2 (по 3 варианта)
- [ ] Расписать 5 вариантов Блока 3 (советы)
- [ ] Расписать 3 варианта Блока 4 (образование)
- [ ] Согласовать с пользователем

### **Реализация:**
- [ ] Изменить layout (2 колонки)
- [ ] Добавить/обновить метрики
- [ ] Реализовать все блоки с динамикой
- [ ] Проверить цветовое кодирование
- [ ] Проверить на устройствах (mobile responsive)
- [ ] Закоммитить

---

## 🔍 Особенности старых карточек

### **Привычки:**
- Метрики: `avgCompletion`, `streak`, `bestDay`
- Текущие варианты: 3 (по `avgCompletion >= 80/60`)
- Проблемы: нет анализа регулярности, нет рекомендаций

### **Шаги:**
- Метрики: `avgDaily`, `totalSteps`, `achievementRate`, `totalDistance`, `totalCalories`
- Текущие варианты: 3 (по `achievementRate >= 80/50`)
- Проблемы: много метрик но мало используются в инсайтах

### **Вода:**
- Метрики: `avgDaily`, `achievementRate`, `streak`
- Текущие варианты: 3 (по `achievementRate >= 70/40`)
- Проблемы: нет паттернов потребления

### **Вес:**
- Метрики: `weightChange`, `trend`, `bmi`, `bmiCategory`
- Текущие варианты: 3 (по `trend` и `weightChange`)
- Проблемы: много условий, сложная логика, есть ИМТ но слабо используется

---

## 📚 Примеры из новых карточек

### **Хороший пример Блока 1 (Сон):**
```tsx
{avgHours >= goal ? (
  <Card className="bg-gradient-to-br from-emerald-500/10...">
    // 🟢 Цель достигнута
) : avgHours >= goal * 0.9 ? (
  <Card className="bg-gradient-to-br from-blue-500/10...">
    // 🔵 Почти у цели
) : avgHours >= goal * 0.75 ? (
  <Card className="bg-gradient-to-br from-amber-500/10...">
    // 🟡 Недосып
) : avgHours >= goal * 0.6 ? (
  <Card className="bg-gradient-to-br from-orange-500/10...">
    // 🟠 Серьёзный недосып
) : (
  <Card className="bg-gradient-to-br from-red-500/10...">
    // 🔴 Критический недосып
)}
```

### **Хороший пример Блока 2 (Настроение - Стабильность):**
```tsx
<Card>
  <div className="flex items-center gap-2 mb-3">
    <Activity className={cn("w-5 h-5", 
      moodStability < 0.8 ? "text-emerald-400" : 
      moodStability < 1.5 ? "text-amber-400" : "text-red-400"
    )} />
    <h3 className={cn("font-bold", 
      moodStability < 0.8 ? "text-emerald-400" : 
      moodStability < 1.5 ? "text-amber-400" : "text-red-400"
    )}>Стабильность</h3>
  </div>
  <p className="text-white/60 text-sm">
    {moodStability < 0.8 
      ? "Ваше настроение очень стабильно..." 
      : moodStability < 1.5 
      ? "Настроение меняется умеренно..." 
      : "Настроение сильно колеблется..."}
  </p>
</Card>
```

### **Хороший пример Блока 3 (Питание):**
```tsx
{currentGoalType === 'loss' && successRate < 40 ? (
  <Card>
    <p className="text-white/80 text-sm">
      💡 <strong>Создайте дефицит постепенно:</strong> Начните с сокращения...
    </p>
  </Card>
) : currentGoalType === 'loss' && successRate >= 70 ? (
  <Card>
    <p className="text-white/80 text-sm">
      ⚡ <strong>Не забывайте о белке:</strong> При дефиците калорий...
    </p>
  </Card>
) : ...}
```

---

## 🚀 Порядок работы (рекомендую)

1. **Привычки** - самая простая, хороший старт
2. **Шаги** - много метрик, интересная
3. **Вода** - средней сложности
4. **Вес** - самая сложная (ИМТ, тренды, категории)

---

## 💾 Git-коммиты

Формат: `feat: refactor [metric] personal insights with extended variants`

Примеры:
```bash
git commit -m "feat: refactor habits personal insights with extended variants"
git commit -m "feat: refactor steps personal insights with extended variants and distance analysis"
git commit -m "feat: refactor water personal insights with consumption patterns"
git commit -m "feat: refactor weight personal insights with BMI integration"
```

---

## ⚠️ Важные моменты

1. **Не добавляй функционал без согласования** - только предлагай
2. **Сначала анализ → предложение → согласование → реализация**
3. **Коммит после каждой карточки**
4. **Динамические цвета для ВСЕХ блоков**
5. **5+ вариантов в Блоке 1** (не 3 как раньше)
6. **Проверяй актуальность** - могли измениться виджеты/функционал

---

## 📁 Файлы для работы

```
app/dashboard/health-tracker/components/stats-details/
├── stats-habits.tsx   ← доработать
├── stats-steps.tsx    ← доработать
├── stats-water.tsx    ← доработать
├── stats-weight.tsx   ← доработать
├── stats-sleep.tsx    ✅ готово (пример)
├── stats-caffeine.tsx ✅ готово (пример)
├── stats-mood.tsx     ✅ готово (пример)
└── stats-nutrition.tsx ✅ готово (пример)
```

---

**Готов начать с любой карточки! С какой начнём?** 🚀
