# MargoFitness

Онлайн-платформа для персональных тренировок с гибридной бизнес-моделью (подписки + разовые покупки).

**Статус:** ✅ Фаза 1 завершена (Backend + Базовый UI)

## 🛠️ Технологический стек

- **Framework:** Next.js 15 App Router
- **Language:** TypeScript (строгая типизация)
- **Styling:** Tailwind CSS v4
- **UI Components:** Shadcn/UI (Radix primitives)
- **Backend:** Supabase (Auth, PostgreSQL, Realtime)
- **Icons:** Lucide React
- **Linting:** ESLint

## 📋 Особенности

- ✅ Server Components (RSC) по умолчанию
- ✅ Строгая TypeScript типизация
- ✅ Утилиты Tailwind CSS
- ✅ Shadcn/UI компоненты (стиль New York)
- ✅ Поддержка темной темы
- ✅ Оптимизация изображений через `next/image`
- ✅ Алиас импорта `@/`

## 🎯 Текущий статус

### ✅ Что готово (Фаза 1)

- **База данных:** 4 таблицы (profiles, products, user_purchases, workouts)
- **Аутентификация:** Email регистрация/вход через Supabase
- **Логика доступа:** Gatekeeper для контроля контента
- **Страницы:** Landing, Workouts, Pricing, Dashboard, Auth
- **Server Actions:** 15+ функций для работы с БД
- **Безопасность:** Row Level Security (RLS) настроен

### 🚧 В разработке (Фаза 2)

- Детальная страница тренировки с Kinescope
- Интеграция платежей (CloudPayments/T-Pay)
- Админ панель
- Интеграция дизайна от Aura.build

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск dev сервера

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### 3. Тестирование

1. Создайте аккаунт: `/auth/signup`
2. Посмотрите тренировки: `/workouts`
3. Проверьте тарифы: `/pricing`
4. Личный кабинет: `/dashboard`

**Подробнее:** см. `PHASE_1_COMPLETE.md`

### Сборка для продакшена

```bash
npm run build
npm start
```

### Линтинг

```bash
npm run lint
```

## 📁 Структура проекта

```
.
├── app/                  # App Router (страницы, layouts)
│   ├── globals.css      # Глобальные стили + CSS переменные
│   ├── layout.tsx       # Root layout
│   └── page.tsx         # Главная страница
├── components/          # React компоненты
│   └── ui/             # Shadcn/UI компоненты
├── lib/                # Утилиты
│   └── utils.ts        # cn() для классов
├── public/             # Статические файлы
└── components.json     # Конфигурация Shadcn/UI
```

## 🎨 Добавление компонентов Shadcn/UI

```bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add input
```

## 🔧 Конфигурация

- **TypeScript:** `tsconfig.json`
- **ESLint:** `eslint.config.mjs`
- **PostCSS:** `postcss.config.mjs`
- **Next.js:** `next.config.ts`
- **Shadcn/UI:** `components.json`

## 📚 Правила разработки

1. **Компоненты:** Функциональные компоненты с TypeScript
2. **Стили:** Tailwind utility classes
3. **Файлы:** kebab-case (`my-component.tsx`)
4. **Компоненты:** PascalCase (`MyComponent`)
5. **Серверные компоненты:** По умолчанию (RSC), `"use client"` только при необходимости
6. **Импорты:** Использовать `@/` alias

## 🔐 Supabase

### ✅ Уже настроено!

Supabase уже полностью интегрирован в проект:

- ✅ Установлены зависимости
- ✅ Настроены переменные окружения (`.env.local`)
- ✅ Созданы клиенты для серверных и клиентских компонентов
- ✅ Настроен middleware для аутентификации

### Быстрый старт

1. **Перезапустите dev сервер** (чтобы применить переменные окружения):
   ```bash
   # Остановите текущий процесс (Ctrl+C)
   npm run dev
   ```

2. **Откройте страницу с примерами:**
   http://localhost:3000/examples

3. **Создайте таблицы в Supabase:**
   - Перейдите в [Table Editor](https://supabase.com/dashboard/project/yxzrenwkkntnhmdimhln/editor)
   - Создайте необходимые таблицы
   - Настройте Row Level Security (RLS)

4. **Сгенерируйте типы:**
   ```bash
   npx supabase gen types typescript --project-id yxzrenwkkntnhmdimhln > types/supabase.ts
   ```

📚 Подробнее: смотрите `SUPABASE_SETUP.md`

## 📄 Лицензия

Private project
