# 🧭 Навигационная панель MargoFitness

## 📋 Описание

Новый минималистичный навбар, интегрированный в Hero секцию страницы. Sticky (прилипает при скролле), адаптивный, с поддержкой мобильного меню.

---

## 🎨 Дизайн

### Особенности

1. **Sticky навигация** - прилипает к верху при скролле с эффектом backdrop-blur
2. **Интеграция с Hero** - навбар является частью Hero секции, не перекрывает контент агрессивно
3. **Glassmorphism** - полупрозрачный фон с эффектом размытия
4. **Плавные анимации** - все переходы оптимизированы для производительности

### Адаптивность

- **Desktop (>1024px)**: полная навигация с ссылками
- **Mobile (<1024px)**: бургер-меню с выдвижной панелью справа

---

## 🔧 Компоненты

### Файлы

- `components/navbar.tsx` - основной компонент навбара
- `components/auth-button.tsx` - кнопка входа (открывает SignInPopup)
- `components/signin-popup.tsx` - модальное окно регистрации/входа

### Использование в Layout

```tsx
import Navbar from "@/components/navbar";
import { getCurrentProfile } from "@/lib/actions/profile";
import { headers } from "next/headers";

export default async function RootLayout({ children }) {
  const profile = await getCurrentProfile();
  const headersList = await headers();
  const pathname = headersList.get("x-pathname") || "";

  return (
    <html lang="ru">
      <body>
        <Navbar profile={profile} pathname={pathname} />
        <main>{children}</main>
      </body>
    </html>
  );
}
```

---

## 🎯 Функционал

### Для неавторизованных пользователей

- Логотип MargoFitness (ссылка на главную)
- Навигация: Главная, Тарифы, Бесплатное
- Кнопка "Войти" → открывает модальное окно SignInPopup

### Для авторизованных пользователей

- Логотип MargoFitness
- Навигация: Главная, Тарифы, Бесплатное, Кабинет
- Для админов: дополнительная ссылка "Админка" (золотой цвет)
- Кнопка с аватаркой → переход в личный кабинет (`/dashboard-new`)

### Мобильное меню

**Триггер:** Кнопка-бургер (3 линии)

**Структура:**
- Backdrop (затемнение фона с blur)
- Выдвижная панель справа (85% ширины экрана, max 448px)
- Заголовок "Меню" с кнопкой закрытия
- Список навигационных ссылок (карточки)
- Футер с кнопкой входа/профилем

**Анимации:**
- Backdrop: fade in
- Панель: slide in from right
- Автоматическое закрытие при изменении размера окна на desktop
- Блокировка скролла страницы при открытом меню

---

## 🎭 Состояния навбара

### Прозрачный (при загрузке страницы)

```css
background: transparent;
border-bottom: none;
backdrop-filter: none;
```

### Sticky (при скролле > 20px)

```css
background: rgba(12, 12, 17, 0.8);
border-bottom: 1px solid rgba(255, 255, 255, 0.1);
backdrop-filter: blur(12px);
```

Переход плавный через `transition-all duration-300`.

---

## 🎨 Цветовая схема

```js
const colors = {
  primary: '#f97316',        // Оранжевый (основной)
  secondary: '#ea580c',      // Темно-оранжевый
  textPrimary: '#FFFFFF',    // Белый текст
  textSecondary: 'rgba(255, 255, 255, 0.7)', // Полупрозрачный белый
  cardBg: 'rgba(255, 255, 255, 0.04)',       // Фон карточек
  cardBorder: 'rgba(255, 255, 255, 0.1)',    // Границы
  adminLink: '#fbbf24',      // Золотой (для админки)
}
```

---

## 🚀 Оптимизация

### Производительность

1. **Passive scroll listener** - не блокирует скролл
2. **CSS animations** вместо JS где возможно
3. **Оптимизированные keyframes** - короткие анимации (0.2-0.3s)
4. **Automatic cleanup** - удаление event listeners в useEffect

### Мобильная оптимизация

1. **Блокировка скролла** при открытом меню
2. **Touch-friendly** - все кнопки имеют `touchAction: 'manipulation'`
3. **Active states** - визуальный feedback на нажатие
4. **Automatic close** - меню закрывается при переходе на desktop

---

## 📱 Responsive Breakpoints

- **Mobile**: `< 1024px` - бургер-меню
- **Desktop**: `>= 1024px` - полная навигация

Используется Tailwind класс `lg:` для переключения.

---

## 🔒 Безопасность

- Профиль загружается через `getCurrentProfile()` на сервере
- Роль пользователя проверяется перед отображением админ-ссылки
- Pathname передается для возможности скрытия навбара на определенных страницах

---

## 🎯 Roadmap (будущее)

### Потенциальные улучшения

- [ ] Dropdown меню для "Тарифы" (с подкатегориями)
- [ ] Поиск по сайту
- [ ] Индикатор активной страницы (highlight текущей ссылки)
- [ ] Уведомления (badge на иконке колокольчика)
- [ ] Анимация логотипа при наведении
- [ ] Переключатель темы (светлая/темная)
- [ ] Мультиязычность (переключатель языка)

---

## 🐛 Troubleshooting

### Проблема: Навбар не появляется

**Решение:** Убедитесь, что в `layout.tsx` передаются `profile` и `pathname`:

```tsx
const profile = await getCurrentProfile();
const headersList = await headers();
const pathname = headersList.get("x-pathname") || "";
```

### Проблема: Мобильное меню не закрывается

**Решение:** Проверьте, что `mobileMenuOpen` state управляется корректно. При клике вне панели (на backdrop) меню должно закрываться:

```tsx
<div onClick={() => setMobileMenuOpen(false)} />
```

### Проблема: Sticky эффект не работает

**Решение:** Убедитесь, что navbar имеет класс `sticky` и `top-0`:

```tsx
<nav className="sticky top-0 z-50">
```

---

## 📄 Зависимости

- `next` - Next.js framework
- `react` - React library
- `lucide-react` - иконки (Menu, X)
- `@/lib/actions/profile` - server action для получения профиля
- `@/components/auth-button` - кнопка входа
- `@/components/signin-popup` - модальное окно входа

---

**Создано:** 29 декабря 2024  
**Версия:** 1.0  
**Статус:** ✅ Production Ready

